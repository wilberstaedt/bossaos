# E14 — VALIDADO a 04/09. Prova local.

Assinado depois de fechar as duas coisas que faltavam abaixo. **Dezoito telas,
14/36 etapas, 38%.** Mantenho o texto da revisão a meio, incluindo a lista do que
então não estava medido, porque o percurso é a parte que se pode auditar.

**Esta validação é LOCAL.** A CI está trancada por facturação do GitHub e não
mediu nada — nenhuma máquina independente confirmou isto. Quem ler «validado»
daqui a um mês tem de saber disso.

## O que medi, eu próprio, na base de revisão

Tudo abaixo correu na `bossaos_revisao` — 30 migrações aplicadas do zero,
incluindo as duas do E14 —, sem tocar na base onde ele trabalha.

**Aceite 3 — «preço novo não altera linhas já aceites»: CONFIRMADO, com o par.**

O gatilho `linha_aceite_imutavel` existe e **morde**:

| ataque | resultado |
| --- | --- |
| mudar o **preço** de linha `ACEITE` | recusado |
| mudar a **quantidade** | recusado |
| mudar a **moeda** | recusado |
| **cancelar** (só o estado) | **passou** — preço 900 e quantidade 1 intactos |
| linha `PROPOSTA` receber preço e ser aceite | **passou** — é o acto de aceitar |

As duas últimas são o que separa esta guarda de uma que bloqueasse tudo. Sem
elas eu teria «provado» um gatilho que impedia cancelar um pedido.

**Falhei a primeira tentativa desta prova** e vale a pena estar escrito: corri os
ataques na mesma transacção, o primeiro abortou-a, e os restantes devolveram
`current transaction is aborted` — que eu podia ter lido como sucesso. Refiz com
pontos de salvaguarda. **População declarada antes de cada afirmação:** 1 linha,
preço 900.

**Idempotência (aceite 1), parte estrutural: CONFIRMADA.**
`order_submissions_command_id_key` é UNIQUE em `command_id`. A garantia é da
base, não de um `SELECT` antes do `INSERT` — que seria a corrida do E13 com
outro nome.

**A metade que impede «recusa sempre»: CONFIRMADA no código.** `pedidos.ts:327`
só declara divergência quando vem preço proposto **e** ele difere. Sem preço
proposto, vale o do servidor e a linha passa.

## O que NÃO medi — e por isso não assino

- **Aceite 1 ao vivo**: reenviar com a mesma `command_id` e receber *a mesma
  resposta*. Verifiquei o índice, não o comportamento do serviço.
- **Aceite 2**: dois operadores a acrescentar ao mesmo tempo sem se apagarem.
- **As 18 telas.** Nenhuma foi aberta num navegador por mim.
- **A suite completa**: estava a correr do lado dele quando escrevi isto.

## Uma observação que NÃO é um defeito

`linha.precoPropostoMenor !== undefined` trataria um `null` explícito como
divergência. O tipo de entrada é `precoPropostoMenor?: number` — não anulável —
e o transporte é formulário, não JSON, por isso **não consegui produzir o
caminho**. Fica dito como observação, não como achado: não chamo defeito ao que
não consegui medir.

## E uma correcção minha, não dele

Ele decidiu **contra o contrato que eu tinha escrito** — e tem razão. Corrigi
`docs/architecture/preco-de-um-pedido-escrito-offline.md`, deixando o texto
errado à vista. Eu tinha inventado uma regra onde já existia uma, e decidido
política comercial que é do dono do restaurante.


---

# O que fechou a assinatura

## As 18 telas, e o controlo negativo que faltava

`pnpm inspeccionar`: **376 verdes**, saída real 0 — lida do comando, não do
invólucro, que na tentativa anterior anunciou «completed, exit code 0» sobre uma
corrida que morreu nas fixtures sem correr um único teste.

Mas 376 verdes não são cobertura. Verifiquei três coisas:

1. **As 18 estão declaradas** em `inspeccao/pedidos.spec.ts` — e a suite **conta a
   própria população**: afirma 18 telas e 18 ids únicos. Uma tela que
   desaparecesse da lista partia o teste em vez de sumir em silêncio.
2. **Recusa redireccionamento** — o endereço final tem de ser o pedido. É a
   defesa contra medir a página de «não encontrado» como se fosse a tela, que é
   o defeito que me tirou cinco assinaturas hoje.
3. **Consegue ficar vermelha.** Parti o cabeçalho do ORD-010 **na página real** —
   não uma sonda — e a inspecção falhou nas **cinco larguras**, saída 1. Reposta,
   árvore limpa, 376 outra vez.

A terceira é a única que transforma uma suite verde em prova.

## Um erro meu pelo caminho, que vale mais do que o resultado

A minha primeira verificação de cobertura deu **zero nas dezoito**. Ia daí para
«o JR não cobriu nada» — mas o total também deu zero, e um instrumento que não
encontra *nada* está avariado, não está a medir. Estava a procurar num ficheiro
que é outra coisa (`alvos.ts` são identificadores lidos da base, não a lista de
telas). É o meu defeito de sempre: **vigiar uma forma de escrita em vez da
propriedade.** O sinal que me salvou foi o zero total.

## Veredicto

Os três aceites medidos por mim na base de revisão, com os seis controlos
negativos do `provar-pedidos.sh` a derrubarem cada um a sua asserção e a reporem
21 casos verdes. As 18 telas medidas no arnês do navegador, com controlo negativo
por degradação da página real.

**Assino o E14.** E registo a excepção: a inspecção do navegador correu na
`bossaos_dev`, não na minha base — a autenticação estoira com 500 contra a base
nova e diagnosticar isso é tarefa própria, que fica em aberto. Correu com o JR
parado, não por baixo do trabalho dele.
