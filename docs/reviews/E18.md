# E18 — VALIDADO em `cfafed7`

> Assinado a 05/09/2026, **depois** da correcção da retenção. Deixo por baixo a
> revisão inteira que reprovou a entrega, incluindo o veredicto RETIDO: o que
> ele corrigiu só se percebe ao lado do que estava errado.

## A prova que assina

Medida na base de revisão (`bossaos_revisao`), na árvore de revisão, contra
`cfafed7`, com a árvore limpa e a máquina livre de outras suites.

| Prova | Verde | Controlos negativos | Falhas |
| --- | --- | --- | --- |
| `provar-reservas.sh` | 13 asserções | **10**, todos a acender | 0 |
| `provar-reservas-no-navegador.sh` | 19 casos (6 telas × 5 larguras, toque, contraste, ES/PT/EN, população) | **7**, todos a acender | 0 |
| `provar-publico.sh` | grupos 1 a 5 | 3 | 0 |
| `provar-visitante.sh` | verde | — | 0 |
| `provar-visitante-no-navegador.sh` (E17, verde caducado) | 23 casos | **10**, todos a acender | 0 |

Entre `cfafed7` e o commit desta assinatura mudaram **zero ficheiros de
produto** — só documentos meus. A assinatura vale para a árvore de hoje.

### Os três aceites da régua, um a um

1. **O intervalo, não o instante.** Grupo 1 do `reservas.test.ts` prova os dois
   lados do limite semiaberto: `[19,20)` e `[20,21)` não se sobrepõem, e uma
   reserva que acaba às 20h31 recusa a das 20h30. A base recusa sozinha as
   sobreposições escritas à mão, e **aceita** as que encostam — sem esse segundo
   caso, «recusa tudo» passava.
2. **A ocupação é partilhada.** Grupo 7: uma sessão de mesa aberta tira a mesa
   da disponibilidade, e um bloqueio de zona fecha as mesas dela sem tocar no
   bar. O controlo negativo «o walk-in deixa de ocupar a mesa» acende.
3. **A hora de Verão.** Grupo 8, e é onde ele foi além do pedido: 02h30 na noite
   do adianto é inexistente e resolve para 03h30; na noite do atraso é ambígua;
   e ele escreveu **sozinho** o discriminador — «uma hora banal é NORMAL, senão
   "tudo ambíguo" passava os dois acima». É o raciocínio que lhe cobro desde o
   E11, agora sem eu pedir.

### O que os controlos negativos protegem

O mais valioso não foi o da agenda vazia (embora acenda: com as listas vazias a
prova fica vermelha, e era a forma de verde vazio mais provável aqui). Foi este:

> **«caiu a navegação: as ligações voltaram a fingir-se de telas»**

É o defeito que me apanhou a mim nesta etapa — o meu controlo negativo passou
verde porque a navegação escreve os IDs de todas as telas em todas as páginas, e
o instrumento estava a medir o menu, não o ecrã. Ele transformou a minha
armadilha num controlo permanente. A partir de agora a prova detecta-a sozinha.

### O que fica pendente, e não bloqueia

As 12 provas que ainda não correm na CI continuam fora dela por **facturação do
GitHub Actions**, que só o Matheus desbloqueia. Está registado; não é dívida
técnica desta etapa.

---



## Achado menor, e não é de dinheiro: um nome ambíguo

`validar-dinheiro.sh` acusa `packages/db/src/reservas.ts:300`:

```
for (const l of linhas) porZona.set(l.area_id, Number(l.total));
```

**Não é defeito de dinheiro.** Fui ver o contexto: aquele `total` é uma contagem
de **pessoas por zona**, e o comentário logo acima explica que a soma é sobre
reservas distintas para não contar um grupo duas vezes. `Number()` sobre uma
contagem é exacto.

**E não vou enfraquecer a guarda por causa disto.** As excepções que ela já tem
são todas por unidade declarada no nome — `centimos`, `cents`, `minor`,
`emUnidadeMinima` — e essa convenção vem do `dinheiro.md`. Acrescentar uma
excepção para «às vezes total não é dinheiro» é como a guarda perde o valor: a
primeira excepção sem regra abre a porta às seguintes.

**O que peço é o nome.** `total` num motor de reservas é ambíguo para quem lê e
para a guarda: renomear para `pessoas` — ou o alias na própria consulta SQL —
resolve os dois e melhora o código. É trabalho de um minuto.

Vale a pena notar que a guarda **passou o seu próprio controlo negativo** na
mesma corrida: *«apanha as três conversões de dinheiro e não acusa as três
legítimas»*. Ela está calibrada; o nome é que não diz o que conta.

## O resto está a ser medido

A suite completa corre na árvore isolada, no commit `e8b7352`, com a máquina
limpa. Há um segundo vermelho — `provar-isolamento-no-produto.sh` — que ainda
não investiguei, e o terceiro é o pendente conhecido da CI.

## Achado: a porta pública deixou de ser só de leitura, e ninguém o decidiu

`provar-publico.sh` falha, e **falha com razão**. Duas asserções:

> `a porta pública não aceita nada além de ler`
> `O QR é um endereço, não uma credencial`

A prova varre `apps/web/app/r` à procura de manipuladores de escrita. Encontra
um: **`app/r/[publicLocationSlug]/api/mesa/route.ts`, com `export async function
POST`.**

### Não é buraco de segurança, e digo-o antes do resto

Li o manipulador. Escreve só depois de `visitanteDaRequisicao()`, e recusa com
`sessao: 'terminou'` se não houver visitante. A acção `entrar` verifica que a
mesa está aberta antes de pôr a bolacha. **A porta está fechada à chave.**

### O que é, então

A rota **mudou de sítio**: o `app/api/publico/mesa/route.ts` que eu li ao validar
o E17 já não existe. O manipulador passou para dentro da pasta pública, e a
mudança até tem lógica — pôr as acções do visitante sob `/r/[slug]/api/` mantém o
âmbito do inquilino no próprio endereço, em vez de um `/api/publico/` global.

**O problema é o silêncio.** Uma asserção de segurança escrita numa etapa
anterior passou a ser falsa, e isso não aparece em declaração nenhuma. A prova é
a única coisa que reparou.

### O que peço

Não peço reverter. Peço **a decisão escrita**: se a pasta pública passa a poder
conter escrita autenticada por sessão de visitante, isso é uma mudança na forma
da superfície pública e merece uma linha no contrato — e a prova passa a afirmar
a regra nova, que já não é «nada além de ler» mas **«nada sem sessão de
visitante»**, com o par: uma escrita sem bolacha é recusada.

Trocar a asserção sem a decisão seria calibrar a guarda ao que existe — o defeito
que passámos o dia a caçar.


---

# Veredicto: RETIDO, por uma coisa só

**O que está bem, e é a maior parte.** As dez famílias de `provas/reservas.test.ts`
com os seus pares; `provar-reservas.sh` verde na minha base; as três provas de
navegador verdes; e a suite completa a 47 de 49 na árvore isolada com a máquina
limpa.

**O que retém** é a `provar-publico.sh`, e não é a funcionalidade: é uma asserção
de segurança de uma etapa anterior que passou a ser falsa **sem decisão escrita**.
A porta do visitante mudou para dentro de `apps/web/app/r`, que a prova declara
só de leitura.

Não é buraco — o manipulador exige `visitanteDaRequisicao()` antes de escrever, e
verifiquei-o. Mas enquanto ninguém escrever a regra nova, ficamos com uma prova
vermelha que a próxima pessoa não sabe atribuir, e uma asserção de segurança
enfraquecida em silêncio.

**Peço duas linhas e uma alteração de prova:**

1. A decisão no contrato: a pasta pública **pode** conter escrita, desde que
   autenticada por sessão de visitante. Com a razão — manter o âmbito do
   inquilino no endereço é melhor do que um `/api/publico/` global.
2. A prova a afirmar a regra nova: já não «nada além de ler», mas **«nada sem
   sessão de visitante»** — com o par, uma escrita sem bolacha recusada.

Trocar a asserção sem escrever a decisão seria calibrar a guarda ao que existe.

## E uma coisa que é minha, não dele

O `git log` atribui a mudança de rota a um commit **meu**. Não é dele que veio o
erro: eu fiz `git add -A docs/` e um `git commit` a seco, e o commit levou o que
ele tinha no índice. Está registado em `COMO-REVISO.md` — **o índice do git é
estado partilhado**, e foi o quinto que encontrámos hoje.

## A decisão que ele escreveu é mais forte do que a minha suposição

Li o que está a entrar no contrato. Eu tinha justificado a mudança de rota com
«manter o âmbito do inquilino no endereço é melhor do que um `/api/publico`
global» — a conclusão certa pela razão fraca.

**A razão verdadeira é que a mudança era obrigatória.** A bolacha do visitante
tem `path=/r/<slug>`; com a porta fora desse caminho, o navegador **nunca a
enviava**, e todos os POST do visitante caíam em silêncio no `?sessao=terminou`.
A porta teve de ir para dentro do endereço do restaurante.

E a alternativa barata — alargar a bolacha para `path=/` — foi rejeitada pelo
motivo que eu não tinha visto: **uma credencial de mesa que viaja para a raiz vai
com cada pedido feito a qualquer outro restaurante servido pelo mesmo domínio.**
O âmbito no caminho não é arrumação: é o que impede a credencial da mesa 5 de
chegar à casa do lado.

Ou seja: o meu achado estava certo a exigir a decisão, e a decisão é melhor do
que a que eu teria escrito. **Foi por isso que pedi a decisão em vez de propor a
minha** — se tivesse escrito a justificação eu próprio, ficava a fraca no
contrato e ninguém saberia da bolacha.

## Nota de validade: o E17 volta a entrar na conta

Ele está a mexer em `inspeccao/visitante.spec.ts` e em
`scripts/provar-visitante-no-navegador.sh` — **provas do E17**, não do E18. Faz
sentido: a rota que mudou é a do visitante, e a prova que a exercita tem de a
seguir.

**Quando ele declarar, corro as provas do E17 outra vez**, não só as do E18. Já
escrevi isto uma vez, no E17, e voltou a aplicar-se pela mesma razão: uma etapa
que mexe no caminho de outra invalida a medição que eu fiz da outra.

É a terceira vez hoje que uma assinatura minha caduca por trabalho posterior. Não
é um problema do processo — é o processo a funcionar: **uma assinatura é sobre um
commit, não sobre uma etapa.** O que seria problema era eu esquecer-me disso.
