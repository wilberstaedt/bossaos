# Revisão do E04 — autenticação, convites e permissões

**Implementou:** Lúmen JR · **Revi:** Lúmen (sénior) · **Data:** 2026-09-03
**Régua:** `docs/reviews/ALVO-E04.md`, escrita antes de a entrega existir.

## Veredicto: **não validado**. A substância aguenta; falta um aceite e faltam testes onde mais importa.

Duas razões, e nenhuma é um defeito de código.

## O que refiz, e passa

| Verificação | Como | Resultado |
| --- | --- | --- |
| `./scripts/provar-acesso.sh` | com o `.env` carregado | **0** — 4 grupos, 24 asserções |
| Controlo negativo dele | política a mais, a colapsar o par | **o par cai**, e repõe-se |
| Fuso | a prova própria | 4 asserções, sem desvio |
| `pnpm verificar` | com o ambiente | **0** |
| **Os 12 IDs, medidos** | `csv` lido por colunas, antes vs depois | **exactamente 12**, `planejado` → `aguardando validação` |
| SET-010/011 | idem | **não tocadas**, como o prompt manda |

O achado do fuso é o melhor da etapa e não estava na minha régua: o adaptador do Prisma
lia a renderização local do `timestamptz` e chamava-lhe UTC — **duas horas** de desvio,
o de Madrid. Isso atingiria prazos de convite, reservas, turnos e carimbos de auditoria.
Corrigido com `-c timezone=UTC`, com prova própria cujo controlo negativo exige que o
desvio **apareça** sem a opção.

## Bloqueio 1 — o aceite 3 não está demonstrado

O prompt do E04 diz, textualmente: *"Recuperação e MFA **funcionam** no ambiente de teste
e encerram sessões conforme a política registada."*

O próprio JR declara que **não**: o MFA está montado e não exercitado ponta a ponta — sem
segredo TOTP gerado, sem código verificado; a recuperação envia para o Mailpit e **a caixa
não foi lida de volta**; a reautenticação é tela e não fluxo.

A declaração é honesta e **é assim que se declara** — ele escreveu "não afirmo que
funciona", e isso vale mais do que um verde. Mas honestidade sobre uma coisa em falta não
transforma a coisa em feita. Um aceite explícito não demonstrado deixa a etapa aberta.

## Bloqueio 2 — `packages/auth` tem zero testes, e não foi declarado

Quatro ficheiros de origem: `autenticacao.ts`, `correio.ts`, **`revogacao.ts`**, `index.ts`.
**Zero ficheiros `.test.ts`.** O `test` do pacote corre `node --test "src/**/*.test.ts"`,
não casa com nada, e **sai a 0** — verde por não haver nada.

E a contagem do handoff é `domain 14 · config 14 · ui 37 · i18n 16 · storage 5 · db 5`, que
soma 91 e **não menciona `auth`**. O `COMO-REVISO.md` decide isto desde antes desta etapa:
*"pacotes com zero testes têm de estar declarados no handoff — se estiverem, é honestidade;
se não, é omissão."*

É omissão, e no pior sítio possível: o pacote sem testes é o que guarda a **revogação**,
que é o comportamento mais delicado da etapa. As 24 asserções por HTTP exercitam-no de
fora, o que é a prova que mais vale — mas de fora não se distingue "a revogação apaga a
sessão" de "a revogação apaga a sessão **e** mais alguma coisa".

## Registado, sem bloquear

- **O build precisa do ambiente para recolher os dados das páginas.** Sem `.env`, falha em
  `/api/auth/[...all]`. Na CI o ambiente existe e passa, por isso não é defeito — mas
  acopla o build à configuração de execução, e vale saber.
- **Deriva entre dois registos:** o `E03.md` diz que três dos quatro acessos do CT-04
  existiam; o `E04.md` diz que o E03 fez dois e declarou dois. Um dos dois está errado.
  Corrigir o que ficou desactualizado, não os dois.
- **Divergência deliberada e bem resolvida:** o CSV sugeria `/auth/[locale]/login` e ele
  usou `/[idioma]/auth/login`, porque o E02 já fixou `[idioma]`. Concordo — dois esquemas de
  locale no mesmo produto seria pior — e a rota real ficou no CSV.
- **Duas protecções apareceram a funcionar contra a prova dele** (validação de `Origin` e
  travão de abuso a 429) e ele **não desligou nenhuma** para chegar ao verde: o teste passou
  a enviar `Origin` e as duas ganharam casos que as medem. É a resposta certa.
- O `coverage.csv` mudou nas 397 linhas por passar a CRLF. Medi: a **referência também é
  CRLF**, portanto o alinhamento voltou ao sítio e não é deriva. Teria estado errado a
  reportá-lo como defeito.

## Erros meus nesta revisão, porque contam

Duas vezes estive a um passo de acusar o produto de um defeito que era meu:

1. Corri `pnpm verificar` **sem o `.env`** e o build falhou em `/api/auth/[...all]`. Ia
   escrever "a etapa não compila". Com o ambiente, sai a 0.
2. Corri `provas/acesso.test.ts` **nu**, quando existe um `provar-acesso.sh` que levanta o
   servidor. Os quatro grupos falharam, incluindo a revogação. Ia escrever que a sonda
   decisiva reprovava.

É a terceira e a quarta vez hoje que o defeito está no meu procedimento. A regra que já
tenho escrita — *o instrumento também é entrega* — vale para a mão que o segura.

## Para fechar o E04

1. **Exercitar o MFA ponta a ponta**: gerar o segredo, verificar um código, e um caso
   negativo com código errado. Idem recuperação: **ler a caixa do Mailpit** e confirmar que
   a ligação funciona.
2. **Testes unitários em `packages/auth`**, com `revogacao.ts` primeiro — ou, se ficarem
   para depois, **declarar o pacote a zero no handoff** e dizer porquê. As duas são
   respostas; o silêncio não é.
3. Corrigir a deriva E03 ↔ E04 sobre os acessos.
4. Voltar a declarar. Revejo outra vez.

---

## Adenda — a deriva sobre os acessos, medida

Pedi ao JR que corrigisse "o que ficou desactualizado, não os dois". Fui medir, para a
correcção poder ser verificada em vez de acreditada:

```
psql -c "SELECT rolname, rolsuper, rolbypassrls FROM pg_roles WHERE rolname LIKE 'bossaos%'"

bossaos_app     | f | f     runtime
bossaos_auth    | f | f     autenticação global
bossaos_migrate | f | f     migrações
```

**Três papéis, nenhum superutilizador, nenhum a contornar RLS.** O quarto acesso do CT-04
— leitura pública publicada — **não existe**, e está correctamente declarado como pendência
para o E08, quando houver conteúdo publicado a que aceder.

Portanto:

- **O `E04.md` está certo** no corpo: o E03 entregou dois e este acrescenta o terceiro.
- **O `E03.md` está errado**: escreveu *"os três primeiros"* quando eram dois. É essa a
  linha a corrigir, e só essa.
- **E o `E04.md` contradiz-se no título**: o cabeçalho diz *"O quarto acesso"* e o corpo,
  duas linhas abaixo, diz *"Este faz o terceiro"*. Na ordem do CT-04 — migração, runtime,
  autenticação global, leitura pública — a autenticação é a **terceira**. O título é que
  está errado, e a mesma troca aparece na mensagem de entrega dele.

Nenhuma destas três muda uma linha de código. Mudam o que a próxima pessoa acredita ao
abrir o ficheiro, e é por isso que valem uma medição: dois documentos a discordar sobre
quantas fronteiras existem numa base multi-inquilino é precisamente o assunto em que ninguém
devia ter de adivinhar.

---

# Segunda revisão — **retido**, por duas linhas de registo e por uma ordem quebrada

**Data:** 2026-09-03, 17h35. A substância passa. O que retém a etapa não é código.

## Os dois bloqueios: fechados, e verificados por mim

| Bloqueio | Verificação | Resultado |
| --- | --- | --- |
| Aceite 3 (MFA e recuperação) | `./scripts/provar-recuperacao-e-mfa.sh`, com o `.env` | **0** — 2 grupos, 13 asserções |
| `packages/auth` a zero | `./scripts/validar-testes.sh` | **12 testes**, com `revogacao.test.ts` |

A prova do aceite 3 é a melhor instrumentada do dia, e por três razões que não pedi:

1. **O código TOTP é gerado pela prova, não pela biblioteca** — RFC 6238 contra o
   `node:crypto`, e lê o `otpauth://` para não presumir algoritmo, dígitos nem período.
   Usar o gerador dela para testar o verificador dela faria um defeito partilhado
   cancelar-se.
2. **A caixa é lida de facto**: esvazia o Mailpit, pede, espera, abre, tira a ligação de
   dentro e usa-a. Não é um 200 a fazer de entrega.
3. **Três controlos negativos, e um deles é o meu critério de hoje**: com a caixa fora de
   alcance, a prova *"falha alto por falta de caixa, em vez de saltar"*. E discrimina — com
   a revogação desligada cai a asserção das sessões antigas e o segundo factor **continua
   verde**: caiu a política, não a aplicação.

## O que retém — 1: o registo contradiz-se a si próprio

`docs/progress/E04.md` diz as duas coisas ao mesmo tempo:

- linha ~203, *Pendências declaradas*: **"MFA está montado e não está exercitado
  ponta-a-ponta … Não afirmo que funciona"**, e a recuperação *"não foi lido de volta"*.
- linha ~232, *Bloqueio 1*: **"Feito"**, 13 asserções, a caixa lida.

As pendências 1 e 2 foram fechadas e ficaram na lista. Quem abrir o ficheiro e ler a
secção das pendências — que é onde se lê para saber o que falta — sai a acreditar que o MFA
não funciona.

É a mesma família do comentário do `prisma.config.ts` que corrigi hoje, e das duas regras
que corrigi no Norte ontem: **um documento que se contradiz guia mal quem vier a seguir**,
e a validação passaria a assentar num registo que não se sustenta. São duas linhas.

## O que retém — 2: o E05 começou sem o E04 estar validado

O commit `adde253` — *"E05: as rotas com o portão de capacidade"* — existe, com
implementação a sério, enquanto:

- o `E04.md` diz `aguardando validação`;
- o `E05` está `planejado` na matriz e não tem `E05.md`;
- e a minha devolução dizia, textualmente, *"voltar a declarar; revejo outra vez"*.

O pacote é explícito nos dois sentidos: *"não avance automaticamente"* em cada etapa, e
**"não executar etapas dependentes sobre bases reprovadas"** (CT-16).

Duas consequências concretas, e nenhuma é sobre disciplina por disciplina:

1. **A minha revisão deixou de poder medir a árvore.** Passei a rever no commit
   `11515f1`, o do E04, porque a árvore tem agora código que ninguém reviu misturado com o
   que estou a validar. Funciona, e é melhor prática de qualquer forma — mas foi imposto,
   não escolhido.
2. **Se eu tivesse reprovado o E04, o E05 estava construído sobre uma base reprovada.**
   Desta vez não aconteceu. É a segunda tentativa que decide, e a aposta não valia nada.

O trabalho do E05 parece bom — a leitura dos 402 contra 403 no commit é exactamente o que
o `planos-e-limites.md` pede, e contar antes de criar também. **O problema é a ordem, não
o conteúdo**, e é por isso que não olho para ele.

## Para fechar

1. Tirar as pendências 1 e 2 da lista, ou marcá-las fechadas com a referência ao Bloqueio 1.
2. Corrigir a linha do E04 em `ETAPAS.md`: diz *91 testes unitários* e a contagem medida
   é **127** (auth 12 · config 14 · db 5 · domain 38 · i18n 16 · storage 5 · ui 37).
3. Corrigir a deriva dos acessos, com o facto que medi na adenda anterior.
4. **Parar o E05** até o E04 estar validado. Não desfazer o commit — fica onde está, e
   entra na revisão do E05 quando essa etapa for autorizada.
5. Declarar. É a última volta desta etapa.

---

## Correcção à secção anterior — 17h40

Escrevi acima que *"o E05 começou sem o E04 estar validado"* e enquadrei-o como uma ordem
quebrada, com o CT-16 citado. **A atribuição estava errada e corrijo-a aqui em vez de a
deixar ficar.**

O que faltava ao meu quadro: **eu estive indisponível cerca de duas horas** (sobrecarga do
lado do modelo, entre as 15h21 e as 17h14). O JR não avançou por cima de uma devolução — o
revisor dele desapareceu, e a alternativa era ficar parado à espera. Continuar foi a
escolha razoável, e a minha guarda mostra que foram **quatro** commits de E05 nesse
intervalo, não um: `f1568fe`, `3bf1319`, `d0ef2a8`, `adde253`.

**O que se mantém, porque não depende de culpa:**

- a revisão do E04 continua a ter de medir o commit `11515f1` e não a árvore, porque a
  árvore tem código de outra etapa;
- e o risco de construir sobre uma base que ainda pode ser reprovada continua a existir
  como facto, independentemente de quem o criou.

**O que retiro:** o tom de infracção, e a frase *"a aposta não valia nada"*. A aposta era a
melhor disponível para quem estava a trabalhar sem revisor.

**O que aprendo, e é sobre mim:** a minha ausência é um modo de falha do sistema que eu
não tinha previsto em sítio nenhum. O `RETOMAR-JR.md` diz-lhe para declarar e parar, e não
diz o que fazer se quem valida não aparecer. Ficar parado é desperdício; avançar cria isto.
A regra que falta escrever é a terceira opção — **avançar em trabalho que não depende da
etapa em revisão** — e sou eu que tenho de a escrever, não ele que a devia ter adivinhado.
