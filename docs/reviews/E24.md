# E24 — RETIDO. Não assinado.

> Declarado em `2aa9c74`. **A retenção é de alcance**, e foi apanhada **antes**
> da assinatura — pela primeira vez, e é essa a diferença que interessa.

## O que está bem, e é substancial

**Foste às fontes.** O ADR `0002-fiscal-espanha.md` cita a AEAT e o BOE com
endereços, e diz o que cada uma disse. **A minha hipótese de que estavas
bloqueado por falta de acesso à web estava errada** — travei-te dezoito minutos
com uma instrução que afinal podias cumprir, e a paragem foi por outra coisa.

**E puseste a pendência nos ecrãs**, não só no ADR — INT-006, POS-020, POS-021,
CAT-021 — com a razão certa: «um limite escrito só num documento de progresso é
um limite que o restaurante nunca lê». Isso é melhor do que eu pedi.

**E os três consertos nos teus próprios controlos**, que descreveste antes de eu
perguntar. O da identidade caía porque tirar só o índice fazia o `ON CONFLICT`
não ter a que se agarrar e **todos** os inserts falharem — o vermelho vinha de
tudo estar partido. **«Um controlo que cai não é um controlo que mede.»**

## A retenção: o ciclo do documento fiscal não tem porta

```
pedirDocumento       chamadas em produto: 0
enviarDocumento      chamadas em produto: 0
responderDocumento   chamadas em produto: 0
corrigirDocumento    chamadas em produto: 0
```

As rotas existem — `pos/<unidade>/fiscal` e `integrations/fiscal` — e chamam
`carregarSala`, `carregarTpv`, `comEscopoDoPedido`. **Carregadores.** Nenhuma
chama uma função de documento, e não há rota de API para documentos.

**Nada, em lado nenhum do produto, pode pedir, enviar, responder ou corrigir um
documento fiscal.** É a classe do `enfileirar` do E19 e do `receberWebhook` do
E23 — e nesta etapa é mais grave, porque o documento fiscal é a razão de a etapa
existir.

## E arrasta uma dívida do E23, que é falha MINHA

`receberWebhook`, `reconciliarComProvedor` e `acontecimentosDaConta` — da **fatia
1 do E23**, que eu assinei — também têm zero chamadores. Nenhum webhook do
adquirente pode chegar ao produto.

**Passou porque a minha varredura leu o commit declarado em vez da etapa.** Está
corrigido em `E23.md` e no método: `scripts/varrer-alcance-da-etapa.sh`, que
varre o **intervalo** e tem controlo sobre si próprio — diz `NAO MEDI` se o
intervalo não devolver ficheiros, em vez de passar a verde.

**Revarri o E20 e o E22 com o método novo:** o E22 está limpo (37 funções, só o
`esquecerPrisma` do E01); o E20 devolve o que já estava documentado.

## O que peço

1. **A porta dos documentos fiscais** — rota de API ou acção de ecrã que chame
   o ciclo, e a prova a percorrê-la.
2. **A porta do webhook do adquirente**, do E23. Sem ela, a `provar-adquirente`
   jura que o reenvio deduplica numa porta que não existe.
3. **A prova que as defende**, com o controlo negativo da forma que já usas:
   tirar a chamada no produto e a prova acender.

Nenhuma destas é trabalho grande. O que era grande já está feito — o motor, os
estados, o rasto por gatilho, e as fontes citadas.

---

## Reportado durante o conserto — a forma certa de abrir uma excepção

*(Do que ele me disse, não de leitura de árvore suja.)*

A rota de webhook **precisa** de excepção à guarda das portas: um webhook não se
alcança por cliques, é o adquirente que chama. Uma excepção mal feita aqui era o
buraco por onde a guarda deixava de valer.

**A frase dele é a regra:**

> «A excepção que criei não dispensa a exigência, **troca-a**.»

Em vez de isentar a rota, exigiu-lhe outra coisa no lugar — que passe pela porta
que confere a assinatura, que leia o **corpo cru**, que **não** use `.json()`, e
que o segredo venha do ambiente.

**O detalhe do corpo cru é o que separa isto de teatro.** Uma assinatura de
webhook é calculada sobre os **bytes** que chegaram. Chamar `.json()` antes de
verificar reserializa, e a assinatura deixa de bater — ou, pior, alguém
"conserta" isso verificando sobre o objecto reserializado, e a verificação passa
a validar aquilo que nós próprios construímos, não o que o adquirente enviou.
**Uma assinatura verificada sobre a nossa reconstrução não verifica nada.**

**E o contador de excepções subiu de 5 para 6, conscientemente e com a razão
escrita.** Um contador que existe para obrigar a olhar só funciona se subir
doer — e se subir em silêncio, não existe.

Isto não levanta a retenção. Mas se as portas aparecerem com esta qualidade, a
etapa fecha bem.
