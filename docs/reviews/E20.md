# E20 — VALIDADO em `ba5619c`

> Assinado a 05/09/2026. Base de revisão reconstruída do zero, cliente
> regenerado, árvore isolada no commit declarado.

## O alcance foi verificado ANTES das provas

Inverti a ordem de propósito. No E19 corri a suite inteira, obtive verde com 35
controlos negativos, e só depois descobri que o `enfileirar` tinha zero
chamadas. **Uma suite verde não responde à pergunta «o produto chega aqui?», e
o silêncio dela parece aprovação.**

| Função | Chamadas em produto | Primeiro chamador |
| --- | --- | --- |
| `agendarPedido` | 1 | `api/org/[orgSlug]/levar/route.ts:75` |
| `horaDeEntrega` | 1 | dentro de `agendarPedido` |
| `preparoDoPedido` | 1 | `mais-tarde.ts:95` |
| `areaQueServe` | 1 | `mais-tarde.ts:191` |
| `conectorDeEntrega` | 2 | — |

## A prova

| Prova | Verdes | Controlos negativos |
| --- | --- | --- |
| `provar-mais-tarde` | 13 | **20** |
| `provar-producao` | 14 | **20** |
| `provar-produto` | 7 | 4 |
| `provar-alvos-e-matriz` | 4 | 4 |

**Zero falhas.** Os controlos que decidem:

- **«caiu o fuso: o momento de produção nasceu duas horas errado»** — a guarda
  contra o regresso do defeito do E19, aqui no fogão.
- **«caiu a derivação: o momento pôde ser escrito de fora»** — larga o gatilho
  da base e confirma que a prova fica vermelha sem ele. Com `trap restaurar EXIT
  INT TERM`, por isso um Ctrl-C não deixa a base sem gatilho.
- **«caiu o relógio: a fila passou a depender do relógio de quem pergunta»**.
- **«caiu a área: um código postal sem área ganhou taxa zero»**.

## O que está bem, e é dele

- **A derivação é um gatilho `BEFORE INSERT OR UPDATE`**, não código da
  aplicação. Um valor escrito de fora é substituído — pela aplicação, por um
  script, ou por um `UPDATE` à mão. Não é uma regra que alguém tem de respeitar:
  é uma coisa que não se consegue fazer.
- **O preparo é o MAIOR das linhas, não a soma.** As estações trabalham em
  paralelo; somar dava produção cedo demais e cozinha a começar trabalho que
  espera meia hora no passe.
- **A hora INEXISTENTE é recusada e não empurrada para a seguinte.** «Aceitar as
  02h30 e servir às 03h30 é combinar uma coisa e fazer outra.» A AMBÍGUA
  aceita-se, e o estado sobe para quem o tiver de dizer.
- **Um grupo que eu não pedi:** «O item que esgota ANTES da hora». Só existe em
  pedidos agendados. A minha régua não o previa e devia.
- **Duas guardas podres consertadas, nenhuma do E20** — incluindo uma que eu
  parti ao assinar o E19.

## Três coisas registadas, nenhuma retém

**1. A hora de entrega não tem limites.** `RecusaDeAgendamento` tem três valores
e nenhum cobre hora no passado, casa fechada, ou data absurda. Uma hora no
passado dá `producao_em` no passado e o pedido **entra já na cozinha**, embora a
pessoa julgue tê-lo agendado.

**Não retenho, e digo porquê:** as sete telas são `/app/` e `/staff/` — nenhuma
é pública, e quem escolhe a hora é do restaurante. **E a minha régua não exigia
isto.** Reter por uma coisa que eu não pedi era mudar as balizas a meio.

**Fica como condição bloqueante da etapa que abrir o takeaway ao público.** Aí o
staff deixa de ser o filtro e a ausência de limites passa a ser defeito.

**2. e 3. As duas arestas do `preparoDoPedido`**, ambas decisões que ninguém
tomou: a **quantidade não entra** (cinco doses contam como uma — certo para uma
fritadeira, discutível para um prato montado), e uma **linha livre contribui
zero** (o «faça-me isto à parte» é justamente o que demora mais). Nenhuma é
defeito; as duas apareceriam em serviço como «a cozinha está sempre atrasada às
sextas» — um sintoma que ninguém liga a uma linha de código.

## Pendências que não são desta etapa

- **A CI continua trancada por facturação.** Prova local. Só o Matheus desbloqueia.
- **Quatro alvos do arnês com forma de lotaria** (`LIMIT 1` sem `ORDER BY`),
  declarados e medidos pelo JR, deliberadamente não tocados porque servem provas
  de etapas assinadas. Vão para o E21.
