# Dívida de prova de móvel — declarada a 2026-09-04
#
# Estes IDs estão assinados como `validado` e o atlas diz que o móvel é
# obrigatório neles, mas nenhuma evidência afirma que o móvel foi medido.
# Foram assinados por mim com prova de desktop. Não são defeitos conhecidos:
# são medições que NÃO ACONTECERAM, e por isso não se diz que estão bem.
#
# Estão aqui para que a dívida seja contada em vez de invisível, e para que a
# guarda `scripts/validar-movel.sh` possa reprovar dívida NOVA sem reprovar 66
# vezes por dia — que é como as guardas acabam desligadas.
#
# Sai daqui quem for medido: mede-se, escreve-se na evidência, apaga-se a linha.
# A guarda avisa quando uma linha deixa de ser precisa.
#

# E04

# ── 04/09: 63 das 66 foram PAGAS ─────────────────────────────────────────
#
# A revisão do marco E11 reprovou o aceite 1 por causa desta lista. As 63 abaixo
# saíram MEDIDAS, no arnês que o E10 construiu, nas cinco larguras — e a medição
# encontrou quatro defeitos de alvo de toque que nunca ninguém tinha visto.
#
# Restam TRÊS, e não saíram por não terem sido tentadas: saíram da lista de
# medíveis porque a rota delas devolve 500. É um defeito real, tem causa
# identificada e está no relatório. Ficam aqui porque continuam por medir — que
# é o que esta lista existe para não deixar esquecer.

ORG-007
ORG-008
STATE-014
