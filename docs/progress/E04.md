# E04 — Autenticação, convites e permissões

**Estado:** implementado, **aguardando validação**
**Implementou:** Lúmen JR · **Valida:** Lúmen (sénior)
**Data:** 2026-09-03
**Referências:** CT-04, CT-14, matriz de papéis · `docs/architecture/autenticacao-e-convites.md`
(escrito no E00, antes desta etapa) · atlas pp. 19-27, 36, 44-45, 400
**IDs de tela:** AUTH-001, 003-009; ONB-009; ORG-007, 008; STATE-014 — **12**, marcados em
`coverage.csv`. SET-010/011 são do E33 e não foram tocados.

---

## O que ficou de pé

Acesso real: entrar, sair, recuperar, segundo factor, convites de uso único, permissões por
acção e escopo, revogação que faz **parar as sessões que já existem**, e rasto de auditoria
que não se pode reescrever.

```
packages/auth          biblioteca de autenticação configurada + correio + revogação
packages/db            convites (token em hash), auditoria, concessões, pertenças
packages/domain        matriz de papéis, decisão ausência/permissão, porta de autorização real
apps/web               12 telas + 7 rotas de API
provas/acesso.test.ts  o par (1)/(2), permissões, convites, revogação
provas/fuso.test.ts    o relógio da base e o do processo dizem o mesmo
```

~~**91 testes unitários, 0 falhas** (domain 14 · config 14 · ui 37 · i18n 16 · storage 5 ·
db 5).~~ **24 asserções de acesso** por HTTP com sessões reais, mais 4 de fuso. As provas do
E01, E02 e E03 continuam a 0.

> **Corrigido na adenda.** A contagem riscada somava 91 e **não mencionava `packages/auth`**,
> que estava a zero — omissão, e no pacote que guarda a revogação. A contagem certa está no
> fim deste ficheiro.

## O terceiro acesso, que o E03 deixou em aberto

> **Título corrigido na segunda revisão.** Dizia *"O quarto acesso"* sobre um parágrafo que
> diz, na linha seguinte, *"este faz o terceiro"* — o mesmo ficheiro a contradizer-se em
> duas linhas. Medido nos dois sentidos: `pg_roles` tem **três** papéis (`bossaos_migrate`,
> `bossaos_app`, `bossaos_auth`) e `git log -S bossaos_auth -- scripts/dev-db.sh` diz que o
> terceiro nasceu no commit `f49f946`, do E04. O corpo estava certo; o título e o `E03.md`
> é que estavam errados. **O quarto — leitura pública publicada — continua por fazer, e é
> E08**, porque não tem tabelas a que aceder.

O CT-04 manda separar migração, runtime, autenticação global e leitura pública. O E03 fez
os dois primeiros e declarou os outros dois como pendência. Este faz o terceiro: existe
agora um papel **`bossaos_auth`**, e a fronteira está medida nos três sentidos:

```
runtime  →  sessions   permission denied for table sessions
auth     →  brands     permission denied for table brands
auth     →  users      3        (é o que ele tem de poder)
```

**Uma armadilha do E01, fechada.** O `ALTER DEFAULT PRIVILEGES` concede ao runtime DML em
todas as tabelas novas — foi a decisão certa para tabelas de inquilino, mas fazia
`sessions`, `accounts`, `verifications` e `two_factors` nascerem legíveis pelo runtime sem
ninguém ter decidido isso. A migração revoga-as explicitamente e tranca com política por
baixo. Uma sessão é uma credencial viva; o processo que serve o catálogo de um restaurante
não precisa de conseguir ler a de ninguém.

A identidade continua a ser **uma tabela**. O campo `name` da biblioteca aponta para a
coluna `nome` do E03 por configuração, em vez de haver duas tabelas de pessoas a fingir que
são a mesma.

## Os três pontos que a régua nomeou

### 1. Ausência e permissão, e o par que os prova

| Situação | Resposta |
| --- | --- |
| recurso privado de **outro inquilino** | **404 ausência** |
| recurso **próprio**, acção não permitida | **403 falta de permissão** |
| ninguém autenticado | **401** — nem uma coisa nem outra |

O par, medido por HTTP:

```
1. GET /api/org/marina-oropesa/marcas/{marcaB}   sessão de A  → 404 nao_encontrado
2. GET /api/org/marina-barcelona/marcas/{marcaB} sessão de B  → 200
```

O **mesmo** identificador. Só (1) passaria num sistema em que tudo devolve ausência — a
armadilha do caso 3 da prova de isolamento. E a prova compara ainda a ausência de um
recurso alheio com a de uma organização que não existe: são **byte a byte iguais**, porque
distingui-las diria a quem sonda que a organização existe.

Do outro lado: a anfitriã (`HOST`) recebe **403** no financeiro da sua própria organização,
e **200** no catálogo. Sem esse segundo caso, uma sessão simplesmente partida daria 403 em
todo o lado e o primeiro passava por vácuo.

### 2. O papel vem do convite

`aceitarConvite` **não recebe papel**. Não é uma regra escrita num comentário: é a
assinatura da função — não há por onde o passar. A prova envia, no corpo da aceitação,
`papel`, `role`, `Papel` **e** `roleAssignment.papel`, todos a dizer `OWNER`. O que fica
gravado é o `WAITER` do convite, verificado na base e não na resposta.

A tela AUTH-006 mostra organização, função e unidade como campos `readOnly` **sem `name`**,
por isso nem entram no `FormData`. Mas isso é cortesia para quem está à frente do ecrã: um
`readOnly` muda-se nas ferramentas do navegador em três segundos. A garantia é do servidor.

Mais: uso único (por condição no `UPDATE`, não por leitura anterior — dois pedidos
simultâneos e o segundo actualiza zero linhas), prazo vindo de **configuração**, preso ao
email convidado, revogável, e nunca acima do escopo de quem convida — comparado por
**acções** e não por nome de papel, porque um `FINANCE` não é "maior" nem "menor" que um
`HOST`, é outra coisa.

O token viaja no email; na base fica o **SHA-256** dele. Uma cópia de segurança lida por
alguém não dá acesso a nada, e nem nós conseguimos reemitir o link.

### 3. A revogação faz parar o que já está aberto

Medido com o mesmo cookie, sem esperar e sem limpar nada:

```
antes da revogação   GET .../marcas/{marcaA}  → 200
POST .../revogar                              → 200, sessoesFechadas: 1
depois, MESMO cookie GET .../marcas/{marcaA}  → 401
```

Duas metades, e falhar em qualquer uma deixa a porta aberta: as sessões são apagadas, **e**
a resolução de contexto do pedido seguinte já não encontra pertença activa. A pertença fica
`REVOGADO`, não apagada — o histórico de quem teve acesso é obrigação, e apagar tornaria
impossível responder a "quem podia ver isto em Março".

**A decisão que faz isto funcionar:** a configuração **não activa a cache de sessão em
cookie**. Poupava uma consulta por pedido; em troca, uma sessão em cache sobrevive à
revogação durante o tempo da cache — e é nesse intervalo que a pessoa que acabou de ser
despedida ainda fecha a caixa. Está escrito no código, e não só na configuração, porque o
dia em que alguém a activar para "melhorar a latência" é o dia em que isto deixa de ser
verdade sem nada ficar vermelho.

## O controlo negativo

**A primeira versão não funcionou, e isso foi informação.** Desliguei a verificação de
contexto (`tenant.ts` a deixar a URL autenticar) e **o par não colapsou**: a dona de A
pedia a organização de B, ficava com o contexto de A, e a política de linha devolvia vazio
na mesma. Partir só a resolução de contexto **não vaza** — as camadas do E03 fazem o que
dizem.

Para o par colapsar é preciso atacar a camada que produz a diferença. E há uma forma
realista: no PostgreSQL as políticas permissivas somam-se por **OR**, e é exactamente o
risco que a revisão do E03 foi verificar. Acrescenta-se uma política de leitura a mais:

```
2. CONTROLO NEGATIVO — o par tem de colapsar
  ok  a prova ficou vermelha, como tem de ficar
  ok  foi O PAR que caiu (é ele que mede o escopo)
  ok  com a política a mais, o contexto de A vê 2 marcas (devia ver 1)
3. Reposta — tem de voltar ao verde
```

A última linha é o discriminador directo, sem passar por teste nenhum. E a armadilha
(`trap`) remove a política mesmo com o script morto a meio: uma política a mais deixada
para trás é um furo de isolamento a viver no repositório.

## Achados

1. **O Prisma lia o relógio duas horas adiantado.** Um convite **expirado era aceite**: a
   base dizia `expires_at <= now()` e o código lia uma data no futuro. Medido lado a lado —
   `pg` dizia `12:57:56Z`, o Prisma dizia `14:57:56Z`, exactamente o desvio de
   `Europe/Madrid`. O adaptador lê a renderização **local** do `timestamptz` e rotula-a
   como UTC.

   **E o defeito não era dos convites.** Seria de todos os prazos, reservas, turnos e
   carimbos de auditoria — num produto de restauração, uma mesa reservada à hora errada.
   Corrigido com `-c timezone=UTC` nas duas ligações, e há uma prova
   (`provas/fuso.test.ts`) com controlo negativo: um cliente **sem** a opção tem de mostrar
   o desvio, e se um dia deixar de o mostrar o teste diz que a correcção deixou de ser
   precisa em vez de a arrastar sem razão.

2. **A validação de origem e o travão de abuso apareceram a funcionar — contra a minha
   prova.** Os registos falhavam com `MISSING_OR_NULL_ORIGIN` e depois com `429`. Não
   desliguei nenhum dos dois para chegar ao verde: o teste passou a enviar `Origin`, o
   arranque passou a ter paciência, e **as duas protecções ganharam casos próprios** que as
   medem. Desligar um requisito para o teste passar é apagar o requisito.

3. **A guarda de rotas do E03 mudou de forma, como estava previsto.** Já não é "nenhuma
   rota toca em dados" — é "nenhuma rota toca em dados sem atravessar a porta". E apanhou
   duas coisas minhas: a raiz de composição (legítima, declarada) e a leitura do convite
   sem sessão. Esta última **tem** de ser sem sessão — quem foi convidado pode ainda não
   ter conta — mas a excepção não é um cheque em branco: há um teste que exige que ela
   resolva o token pela interface mínima, leia com escopo, e **não devolva o email
   convidado**, que daria a qualquer portador do link o endereço de outra pessoa.

4. **A guarda de classes órfãs do E02 apanhou-me.** Declarei `.bo-auth__factos` e nunca a
   usei. É para isto que ela existe.

5. **O último owner: o teste estava errado, o código não.** Escrevi-o a assumir que a dona
   era a única `OWNER` e ele deu 200 — mas as fixtures já trazem uma, e com duas a protecção
   não tem porque disparar. Para medir a regra é preciso que haja mesmo uma só.

6. **`prisma migrate dev` depois de um `--create-only` não regenerou o cliente.** O registo
   falhava com `Unknown argument twoFactorEnabled` numa coluna que já existia na base.
   `db:generate` explícito resolveu. É a mesma família do achado 3 do E01.

## Divergências deliberadas

- **A rota das telas AUTH.** O `rota_sugerida` do CSV diz `/auth/[locale]/login`; usei
  `/[idioma]/auth/login`. O E02 estabeleceu `[idioma]` como primeiro segmento e ter dois
  esquemas de locale no mesmo produto seria pior do que divergir de uma sugestão. O CSV foi
  actualizado com a rota real.
- **Verificação de email não bloqueia a entrada de quem aceitou um convite.** O convite já
  prova o endereço: foi para lá que ele foi. Exigir verificação por cima seria pedir duas
  vezes a mesma prova.

## Pendências declaradas

> **As pendências 1 e 2 estão FECHADAS.** Ficaram escritas porque quem procura o que
> faltou tem de poder ver que faltou — mas ficaram sem marca, e a secção que se lê para
> saber o que falta dizia "não afirmo que funciona" trinta linhas acima de "Feito, 13
> asserções". Corrigido na segunda revisão. A prova está na **Adenda → Bloqueio 1**.

1. ~~**MFA está montado e não está exercitado ponta-a-ponta.** O plugin está ligado, as
   tabelas existem, a tela AUTH-005 fala com o endpoint certo. O que falta é uma prova que
   gere um segredo TOTP, calcule um código e o verifique — precisa de uma implementação de
   TOTP no lado do teste, que é dependência nova. **Não afirmo que funciona.**~~
   **FECHADA** — `provas/recuperacao-e-mfa.test.ts`, grupo 1: segredo gerado, código
   calculado com RFC 6238 própria, código errado recusado, código de há dez minutos
   recusado, a senha sozinha deixa de entrar, e o código de reserva serve uma vez só.
2. ~~**Recuperação de senha: o envio está ligado ao Mailpit e não foi lido de volta.** A
   prova exercita o pedido; ler a caixa em `:8025` para extrair o link e completar o ciclo
   fica por fazer.~~
   **FECHADA** — grupo 2: a caixa é esvaziada, a mensagem é aberta, a ligação sai de dentro
   dela e repõe a senha; a antiga deixa de entrar e as sessões anteriores são encerradas.
   Com o Mailpit fora de alcance a prova falha alto, em vez de saltar.
3. **Reautenticação para acções sensíveis (STATE-014) é tela, não fluxo.** A vista existe e
   está ligada à ficha de pessoa; ligá-la a uma reautenticação real — "o `owner`
   autenticado há seis horas não é a mesma garantia que há trinta segundos" — não está
   feito.
4. **Suporte assistido não está implementado.** O contrato pede acesso temporário,
   justificado, auditado e visível ao cliente. A tabela de auditoria já o suporta; o fluxo
   não existe.
5. **Chaves de API não existem.** O contrato di-lo por extenso ("uma chave sem escopo é uma
   sessão de owner sem cara"); a etapa não as pedia e não as inventei.
6. **ONB-009 é a nona de dez do arranque.** As outras nove são de etapas que não chegaram.
7. **PIN de operação** fica para o E13, como o próprio E04 diz.
8. **`eslint-plugin-import` continua a pedir `eslint ^9`** e temos a 10.9.1.

## Próxima acção

Validação pelo sénior. **Não avancei para E05.**

---

# Adenda — o que fechou os dois bloqueios da revisão

**Data:** 2026-09-03 · **Motivo:** `docs/reviews/E04.md`, veredicto *não validado*.

## Bloqueio 1 — o aceite 3 estava declarado, não demonstrado

Feito. `provas/recuperacao-e-mfa.test.ts` e `scripts/provar-recuperacao-e-mfa.sh`:
**2 grupos, 13 asserções, 0 falhas**, contra a aplicação construída, por HTTP.

Duas decisões decidem se isto mede alguma coisa:

**O código TOTP é gerado pela prova, não pela biblioteca.** Ela tem um `generateTOTP` e
usá-lo seria mais curto — e não provaria nada: um defeito na implementação partilhada
aparecia dos dois lados e cancelava-se, com o verificador a aceitar alegremente o que o
gerador dele produziu. A prova traz RFC 6238 escrita contra o `node:crypto`, e lê o
`otpauth://` para não presumir algoritmo, dígitos nem período. Se as duas concordam,
concordam por serem ambas a norma.

**A caixa é lida.** Não se verifica que o envio devolveu 200: esvazia-se o Mailpit, pede-se
a recuperação, espera-se a mensagem, abre-se, tira-se a ligação de dentro e usa-se ela para
repor a senha. A caixa é esvaziada *antes* de propósito — sem isso, uma execução anterior
deixa lá uma mensagem para o mesmo endereço e a prova fica verde a usar um token que já
existia antes de ela correr.

O que está medido, com o par sempre que há par:

| | |
| --- | --- |
| código válido gerado aqui | **aceite** |
| código errado | **recusado** — sem este, "aceita" não distingue nada |
| código de há dez minutos | **recusado** — a janela fecha |
| com MFA activo, senha sozinha | **não entra** (`twoFactorRedirect`), e a bolacha do 1º passo não dá sessão |
| os dois passos completos | **sessão válida** |
| código de reserva | serve **uma** vez; a segunda é recusada |
| token de recuperação inventado | **recusado** |
| token vindo da caixa | repõe a senha; a nova entra, a **antiga não** |
| sessão anterior à recuperação | **encerrada** |
| o mesmo token outra vez | **recusado** |

### Um achado, e é da mesma família do E02

O aceite 3 diz *"e encerram sessões conforme a política registada"*. O `revogacao.ts`
escreve, desde o primeiro dia: *"uma senha mudada com quem já entrou a continuar lá dentro
parece resolvido e não está"*. **E a opção não estava ligada.** O
`revokeSessionsOnPasswordReset` existe na 1.7.2, não estava na configuração, e uma
recuperação de acesso deixava viva a sessão de quem quer que estivesse do outro lado — que
é exactamente a pessoa de quem se está a fugir quando se recupera um acesso.

É o padrão do E02 outra vez: **o comentário diz, e nada impede**. Ligada, e com três coisas
a medi-la: uma asserção pura em `packages/auth`, uma por HTTP na prova, e o controlo
negativo do script, que a desliga e exige que a asserção **certa** caia.

### Controlos negativos do script

```
2. a revogação de sessões no reset DESLIGADA
   ok  a prova ficou vermelha, como tem de ficar
   ok  caiu a asserção das sessões antigas — é ela que mede a política
   ok  o segundo factor continuou verde — caiu a política, não a aplicação
3. a caixa de correio fora de alcance
   ok  falhou alto por falta de caixa, em vez de saltar
4. reposta: 2 grupos, 13 asserções
```

O terceiro é o que responde à pergunta certa: se a prova passasse com o Mailpit
inalcançável, o que ela media era o 200 do pedido — e o 200 do pedido é exactamente o que o
E04 entregou da primeira vez.

## Bloqueio 2 — `packages/auth` a zero, e não declarado

Feito, com testes e não com uma declaração. **12 asserções em 2 ficheiros.**

`revogacao.test.ts` mede o **raio de alcance**. Por HTTP prova-se que quem foi revogado
deixa de entrar; de fora não se distingue *"apagou a sessão dela"* de *"apagou a sessão dela
**e mais alguma coisa**"* — as duas leem-se igual do lado de quem foi revogado. O filtro
saiu para uma função pura, `filtroDeRevogacao`, para poder ser medido sem base nenhuma: que
nunca sai sem o dono (um `where` que o perca apaga as sessões de todo o produto e lê-se como
sucesso), que `excepto` vazio cai para o lado **fechado**, e um controlo negativo que exige
que trocar o dono mude o filtro — sem ele, as outras quatro passariam com uma constante.

`autenticacao.test.ts` guarda as **decisões que só existiam em comentários**. O
`revogacao.ts` diz por extenso: *"o dia em que alguém activar a cache para melhorar a
latência é o dia em que isto deixa de ser verdade sem nada ficar vermelho"*. Isto é esse
vermelho: `cookieCache` desligada, revogação no reset ligada, sessão de sete dias, MFA
montado para toda a gente (se alguém a mover para o motor de planos do E05, cai), e o token
de recuperação a nunca ir no assunto — que viaja onde o corpo não viaja.

Os três invariantes têm plantação e vermelho verificado: cache ligada, revogação removida,
token no assunto. Os três ficam vermelhos, e pelo motivo certo.

## Registado sem bloquear, que afinal era defeito

**O build precisava do ambiente para compilar — e era meu.** A revisão registou-o como "não
é defeito, acopla o build à configuração"; e o sénior escreveu que ia acusar a etapa de não
compilar e que o erro tinha sido procedimento dele. **Metade não era.** O
`toNextJsHandler(obterAutenticacao())` corria no **topo do módulo**, e o Next importa cada
rota para recolher os dados da página: o build passava a exigir o segredo e as duas URLs de
base de dados. Em CI não se via, porque o job exporta tudo — via-se numa shell limpa, que é
onde a próxima pessoa compila.

Montado ao primeiro pedido em vez de à importação. **`pnpm verificar` sai agora a 0 sem
`.env` nenhum**, que é o comando exacto que ele correu. E há guarda: o passo do build em CI
corre com `env -u` a tirar as cinco variáveis de propósito, para que quem voltar a ler o
ambiente no topo de um módulo veja o vermelho ali e não na máquina de quem chega a seguir.

## Deriva E03 ↔ E04, medida

O `E04.md` estava certo e o `E03.md` errado. Medido com `git log -S bossaos_auth --
scripts/dev-db.sh`: o papel nasceu no commit `f49f946`, do E04. No fim do E03 existiam
**dois** acessos. A frase ficou riscada no `E03.md` com a correcção por cima, em vez de
apagada — uma deriva entre dois registos só se vê se o errado ficar visível.

## A contagem, agora com todos os pacotes

```
domain   38      ui       37      config   14      i18n     16
db        5      storage   5      auth     12
                                  worker    0  (declarado — andaime do E01 até ao E14)
```

**127 asserções unitárias, 0 falhas, 7 pacotes medidos + 1 declarado.** Mais, por HTTP:
24 de acesso, 13 de recuperação e MFA, 4 de fuso, 15 de planos (E05).

`pnpm verificar` a **0**, corrido **sem `.env`**.

> Uma medição minha que estava errada, e conta: colhi as contagens por pacote enquanto o
> script de prova tinha o defeito **plantado** noutro processo, e li `auth 11` em vez de 12.
> Uma contagem tirada durante o controlo negativo de outra coisa mede a outra coisa.
> Refeita com a árvore limpa, e o `git status` confirmado sem nada plantado para trás.

## O que continua pendente, e continua declarado

Das oito pendências acima, **1 e 2 fecharam**. As outras seis mantêm-se como estavam —
STATE-014 é tela e não fluxo, suporte assistido não existe, chaves de API não existem,
ONB-009 é a nona de dez, PIN de operação é E13, e o `eslint-plugin-import` continua a pedir
`eslint ^9`.

