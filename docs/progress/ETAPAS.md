# Estado inicial

Os PDFs, contratos e prompts estão preparados. Nenhuma etapa de implementação foi executada nesta entrega. Copie esta referência para docs/progress quando iniciar o repositório.

| Etapa | Status | Evidência |
| --- | --- | --- |
| E00 | validado | docs/reviews/E00.md — **julgado pelo JR a 05/09, não por quem o escreveu** · 17 contratos a 03/09; hoje **11 intactos, 6 emendados** (o dossiê media 15/2 ao fim de 13 etapas — a erosão continuou) · critério cumprido: `dinheiro.md` de 03/09 ainda decide código do E26 por via do `validar-dinheiro.sh` que dele descende · a prova mais forte é a retenção em `capacidade-e-reservas.md`: o contrato deu DUAS saídas, quem implementou escolheu a da leitura e escreveu porquê, e o varredor ficou redundante em vez de em falta · **contra, e pior do que o dossiê dizia:** o fuso não passou por baixo de contratos que calavam o assunto — passou por baixo da secção Tempo do `capacidade-e-reservas.md`, que desde 03/09 nomeia a hora que não existe e a hora ambígua; o contrato estava certo e era INERTE · **a frase «contrato escrito antes reduz defeitos» fica REPROVADA** por não poder ser desmentida, e reescrita: só impede quem disser a consequência operacional e a regra de decisão NA FRONTEIRA, ou quem ganhar verificador executável; e nenhum contrato apanha função sem chamador — isso é mecanismo |
| E01 | validado | docs/progress/E01.md · 16 testes · provar-separacao-de-credenciais.sh e provar-prontidao.sh a 0 falhas |
| E02 | validado | docs/progress/E02.md · 2ª declaração após docs/reviews/E02.md · 69 testes unitários + 69 no browser · 12 capturas · STATE-001/002/003/005/007/016 |
| E03 | validado | docs/progress/E03.md · 2ª declaração após docs/reviews/E03.md · 85 testes + 28 asserções de isolamento + 12 verificações no provar-isolamento.sh (inclui autoteste do verificador) · zero telas, coverage intacto |
| E04 | validado | docs/progress/E04.md · 3 migrações · **127 asserções unitárias à data da validação** (domain 38 · ui 37 · i18n 16 · config 14 · auth 12 · db 5 · storage 5; worker declarado a zero) + 24 de acesso por HTTP + 13 de recuperação e MFA + 4 de fuso · provar-acesso.sh a 0 com o par a colapsar e provar-recuperacao-e-mfa.sh a 0 com três controlos negativos · 12 telas |
| E05 | validado | docs/progress/E05.md · 3 migrações (planos, descida agendada, plataforma) · **133 asserções unitárias** (domain 38 · ui 39 · i18n 16 · config 14 · auth 12 · db 5 · storage 5 · worker 4; **nenhum pacote declarado a zero**) + 17 de planos + 14 de descidas + 10 de plataforma · provar-planos.sh, provar-descidas.sh e provar-plataforma.sh a 0, com nove controlos negativos ao todo · 12 telas |
| E06 | validado | docs/progress/E06.md · 4 migrações (onboarding, criar organização, quota de marcas) · **motor de horários com três respostas** e lista de arranque com quatro estados · provar-onboarding.sh a 0 com 4 controlos negativos + guarda de limpeza · 14 telas |
| E07 | validado | docs/progress/E07.md · migração `e07_catalogo` (15 tabelas, allergens só de leitura para o runtime) · **ausência de declaração vale DESCONHECIDO por tipo, não por convenção** · empate de preços recusa em vez de escolher · modificadores validados por chamada directa à API, com os limites lidos da base · dinheiro em inteiros de unidade mínima · `provar-catalogo.sh` a 0 com 7 grupos, 21 asserções e 6 controlos negativos · guarda nova `validar-classes.sh` · 18 telas |
| E08 | validado | docs/progress/E08.md · migração `e08_media_traducoes_importacao_publicacao` (8 tabelas; `menu_revisions` sem UPDATE nem DELETE para o runtime) · **publicar é atómico por construção** — a revisão e a troca do ponteiro no mesmo COMMIT · CSV neutralizado, SVG recusado pelos bytes, três portas contra a rede interna · nome igual nunca funde produtos · exportação verificada duas vezes · 221 asserções no domínio + 35 contra a base, **34 controlos negativos** · retido à 1ª: o `redirect: manual` era uma protecção sem vigia, fechada com o controlo 9d · 10 telas |
| E09 | validado | docs/progress/E09.md · provar-publico.sh: 11 passos, **sete grupos, 26 casos, dez** controlos negativos · retido uma vez pela detecção de verbo que só via uma forma de escrever POST, fechada com onze formas · endereço público que não volta ao mundo, com o par que o separa da regra preguiçosa (o dono retoma) · **ASSINATURA PARCIAL fechada a 04/09**: as 5 telas que exigiam sessão (CHAN-001, QR-001/003/004, REP-001) foram medidas no E10, quando o arnês passou a autenticar · a correcção da fuga entre unidades ganhou detector próprio (grupo 7) depois de se descobrir que a própria prova a repunha na base a cada passagem |
| E10 | validado | docs/progress/E10.md · provar-sites.sh: 11 passos, 27 casos, **8 controlos negativos** — rascunho que sai, retirada por bandeira, idempotência do lead, **o `catch` largo**, domínio largado, a **regra preguiçosa** e a porta que serve sem prova de controlo · **254 casos de navegador corridos pelo revisor** a 360/390/768/1280/1440 · as 29 telas com móvel medido, e as **5 do E09 saíram da dívida** — condição bloqueante cumprida |
| E11 | validado | docs/reviews/E11.md · **marco do Starter APROVADO à 2ª** · reprovado à 1ª com **seis** falhas, todas de MEDIÇÃO e duas de assinatura minha · fechadas e reprovadas por `scripts/provar-marco-e11.sh`, que responde às seis pelo nome · 112/112 telas validadas **e** medidas em móvel · recusa entre inquilinos vista no ecrã nos dois sentidos · jornada percorrida de organização inexistente até site publicado · identidade por porta estreita com os dois consertos errados plantados |
| E12 | validado | docs/progress/E12.md · **validado à 2ª** — a 1ª assinatura foi retirada por eu ter assinado sobre um verde só local · a CI passa agora o trabalho do navegador, que era o ambiente que me contradizia · 7 controlos negativos na base e 22 casos no navegador com a cor **calculada** · a prova deixou de HERDAR o plano: estabelece-o e **verifica-o antes de medir** |
| E13 | validado | docs/progress/E13.md · **validado à 2ª** — retido por a junção a `users` do `ORG-007` estar viva pela **terceira** vez, no `responsavel` do `salaAgora` · fechada por estrutura com `validar-juncao-identidade.sh`, que a encontrou um minuto depois de existir · aceite 1 atacado pelo revisor **contra a base**, nos dois lados: segunda sessão recusada pelo índice único **parcial**, e a mesa reabre depois de FECHADA · limpeza como estado da sessão, não coluna da mesa |
| E14 | validado | docs/progress/E14.md · 2 migrações (`e14_pedidos` com 5 tabelas, RLS, envios e eventos **append-only por privilégio**, e o gatilho `linha_aceite_imutavel`; `e14_combos` com a restrição `componente_de_combo_nao_tem_preco`) · `provar-pedidos.sh` a 0 com **3 grupos, 21 casos e OITO controlos negativos** · idempotência por **restrição única** na base — o reenvio depois do commit devolve a MESMA resposta, e duas chaves diferentes continuam a criar dois pedidos · linhas **acrescentadas** e nunca reescritas em bloco; conflito **recuperável** com o que mudou e por quem · preço **copiado** para a linha aceite, com gatilho a recusar alterá-lo · **18 telas** (a régua diz 20; a matriz tem 18 e as outras 3 são telas anteriores a rever) com móvel medido · **a pergunta de dinheiro fica respondida**: o preço é o do servidor ao aceitar, e a divergência é rejeitada com o motivo em vez de aplicada em silêncio |
| E15 | validado | docs/progress/E15.md · `packages/fila`: partição por org+unidade+utilizador **como chave do armazém**, «não enviado» distinto de «pendente», gravação a cada transição, consulta-antes-de-repetir, portão de sessão · `provar-fila.sh` a 0 com **11 grupos, 24 casos e DEZ controlos negativos** · **23 telas** medidas no navegador às cinco larguras, alvos de operação a 48 px, contraste e três línguas — e a prova **conta a própria população** (23 telas, 23 ids, comparados contra a matriz) · `provar-staff-no-navegador.sh` com **cinco defeitos plantados no artefacto real** · PWA: manifesto por unidade e *service worker* servido de dentro do âmbito, que **nunca guarda HTML autenticado** — num tablet partilhado isso era a regra 3 quebrada por uma cache · `devices.rascunhos_por_enviar` **ligado à fila**, fechando a pendência que o E13 declarou · divergência de preço do E14 **visível no ecrã do empregado**, com os dois números lado a lado · prova **LOCAL**: a CI está trancada por facturação |
| E16 | validado | docs/progress/E16.md · migração `e16_producao_e_estacoes` (4 tabelas, RLS, **três gatilhos** — o estado de produção recusado no pedido, a versão da tarefa monótona, o carimbo de criação do servidor — e o `CHECK` de um alvo por regra) · **a unidade da estação é a TAREFA**: um-para-muitos desde o primeiro dia, e o modelo ingénuo é o primeiro controlo negativo · ausência de regra dá **não encaminhado**, nunca «cozinha por omissão» · o estado de produção do pedido **deriva** e a base recusa quem o escrever · o limite visível é regra de ECRÃ e não entra na consulta · os temporizadores contam do relógio da **base**, nunca do tablet · `provar-producao.sh` a 0 com **11 controlos negativos** e `provar-kds-no-navegador.sh` a 0 com **sete** · **20 telas** medidas às cinco larguras, alvos de operação a 48 px, contraste e três línguas, com a população lida da matriz · prova **LOCAL**: a CI está trancada por facturação |
| E17 | validado | docs/progress/E17.md · migração `e17_qr_da_mesa_e_visitante` (`guest_sessions`, RLS, e **três portas estreitas**: `visitante_activo`, `mesa_do_qr`, `abrir_visitante`) · **rodar não é revogar**, e a garantia é um campo que NÃO existe — a sessão de visitante não guarda a geração do QR, logo a comparação que faria a rotação expulsar gente não tem o que comparar · **mesa fechada com QR válido não abre sessão**: a porta só devolve linha com sessão de mesa ABERTA · o estado do visitante é DERIVADO · pedido de visitante é sempre de origem CARTA · **chamar a sala e pedir a conta** com limite, deduplicação e confirmação: limite e dedup são a MESMA janela de 120 s, por **MESA** e não por telemóvel (uma fotografia do QR abre telemóveis sem conta), na porta `chamar_a_sala` em plpgsql com `FOR UPDATE`, e quem chama vê **três respostas distintas** — avisámos agora / já tínhamos avisado / alguém vem a caminho · a bolacha do visitante tem `path=/r/<slug>` e a porta mudou para dentro desse caminho: fora dele o navegador não a enviava e **todos** os POST do visitante caíam em silêncio · `provar-visitante.sh` a 0 com **nove controlos negativos** — o obrigatório verifica também que o caso da revogação **não** cai junto — e `provar-visitante-no-navegador.sh` a 0 com **nove** · **16 telas** medidas às cinco larguras, com o caminho final afirmado em cada visita, que é o que separa medir a tela de medir o desvio · correcção de arnês transversal: o fecho da semeadura reconhecia o seu lixo pelo nome e os **pedidos** entram pela porta real com número da sequência — 398 ficaram na base a prender as estações, e a auto-verificação, calibrada pelo mesmo crachá, dizia «nada ficou para trás» · prova **LOCAL**: a CI está trancada por facturação |
| E18 | validado | docs/progress/E18.md · migração `e18_reservas_e_capacidade` (8 tabelas, RLS, e a garantia central na FORMA: `EXCLUDE USING gist (table_id, tstzrange(inicio, fim, '[)'))` — o `'[)'` **é** a regra do intervalo semiaberto, e não existe outro sítio onde possa ser escrita ao contrário) · **a unidade de alocação é a MESA**: uma combinação escreve uma linha por componente, e «3+4 e depois só a 3» bate na mesma exclusão sem caso especial · o **lock por unidade** guarda o que nenhuma restrição exprime, a CONTAGEM de comensais por zona · **o controlo obrigatório mudou por medição**: sem lock mas com `serializable` o teste fica verde COM RAZÃO (o SSI apanha o mesmo desvio e o retry responde), por isso o controlo desliga a serialização inteira — e o cenário passou a pedir 2 e 4 para caírem em mesas DIFERENTES, porque 4 e 4 escolhiam a mesma e media-se a exclusão a chamar-lhe contagem · a exclusão só é alcançável por escrita directa, e tem dois casos que a exercem (recusa a sobreposição, aceita o encosto) · a retenção expira por RELÓGIO e o varredor é higiene · hora INEXISTENTE e AMBIGUA resolvidas pela base, com o estado devolvido para o ecrã poder perguntar · depósito desligado por decisão, com `CHECK` a segurá-lo · `provar-reservas.sh` a 0 com **dez controlos negativos** e `provar-reservas-no-navegador.sh` a 0 com **seis** · **6 telas** medidas às cinco larguras, com guarda de população porque uma lista vazia é o ecrã fácil · prova **LOCAL**: a CI está trancada por facturação |
| E19 | validado | docs/progress/E19.md · migração `e19_lista_de_espera` (estado `SENTADO`, carimbos com `CHECK` a exigi-los, e `waitlist_areas` para as zonas que servem o grupo) · **a garantia é uma AUSÊNCIA**: não há coluna `posicao`, `numero_na_fila` nem `senha`, e há um caso a ler o `information_schema` mais um controlo que põe a coluna de volta · a posição **deriva-se** e agrupa pelo CONJUNTO DE MESAS que serve o grupo, com a preferência de zona na mesma chave — «o 2.º dos grupos de 5 ou 6» sobrevive ao de 2 passar à frente · quem não cabe em mesa nenhuma tem posição `null`, e não o último lugar · o par provado nos dois sentidos: sentar o de 2 muda a posição do outro de 2 **e não muda** a do de 6 · o controlo obrigatório do contrato acende e está **confinado** pela sugestão ao host, que tem de ficar verde · a estimativa nunca viaja sem `estimativa: true` ao lado · `provar-espera.sh` a 0 com **oito controlos negativos** · **11 das 28 telas feitas** (o fluxo público: PUB-003 e RES-C-001..010), medidas às cinco larguras com 9 controlos negativos · a porta da reserva pública fica FORA de `/r/` por decisão escrita — o que justifica o âmbito no endereço é haver uma credencial que não pode viajar, e um formulário anónimo não tem nenhuma · limite por unidade e janela com lock, chave idempotente nascida no HTML, e a recusa a levar ALTERNATIVAS consigo · a estimativa e o facto medidos no TEXTO, e a estimativa desaparece quando a mesa está pronta · **22 das 28 telas feitas**: o fluxo público (11) e o host (11), mais o `FLOOR-006` REVISITADO · migração `e19_chegada_e_lugar`: **chegar não é estar sentado** — dois actos, dois carimbos, e a base exige `chegou_em` em CHEGOU e em SENTADA · a reserva que aí vem aparece na sala numa mesa MESMO livre, com o par (a de amanhã não aparece, a já sentada deixa de ser anunciada) · as atrasadas são DITAS e não varridas, e a agenda di-lo por palavras · a definição das chegadas está ao lado do número · `provar-host.sh` a 0 com **sete controlos** e `provar-host-no-navegador.sh` a 0 com **seis** · **as 28 telas feitas**, mais o `FLOOR-006` revisitado · a mensageria: a MENSAGEM existe uma vez por (reserva, tipo) e as TENTATIVAS penduram-se nela, logo reenviar acrescenta uma tentativa e a entrega é um carimbo que não se escreve duas vezes — com o par, uma mensagem DIFERENTE para a mesma reserva É enviada · o conector nasce desligado e `activo` sem `provedor` não é um estado (`CHECK`), e sem provedor a mensagem fica PENDENTE, nunca ENVIADA · cada número do relatório traz a definição ao lado, e as duas telas que o mostram dizem o mesmo · `provar-mensagens.sh` a 0 com **sete controlos** e `provar-mensagens-no-navegador.sh` a 0 com **seis** · `pnpm inspeccionar` a **517 casos verdes** · pendência declarada: nenhuma mensagem foi entregue a ninguém, não há provedor · prova **LOCAL**: a CI está trancada por facturação |
| E20 | validado | docs/progress/E20.md · migração `e20_pedidos_para_mais_tarde` · **o momento de produção é DERIVADO** (hora de entrega menos preparo) e um gatilho substitui o que venha de fora · o preparo é o MAIOR das linhas e não a soma, porque as estações trabalham em paralelo · a passagem é por RELÓGIO e o relógio é o da BASE; a tarefa existe desde o aceite, logo atravessar o momento duas vezes não cria duas entradas · a hora de entrega passa por `resolverHoraLocal` e **sem fuso recusa-se**; uma hora que não existe é recusada, não empurrada · **a prova mede hora ABSOLUTA contra o relógio da base**, com o par dos dois fusos — nunca minutos relativos · o canal é uma DIMENSÃO (`DELIVERY` no enum) e não uma tabela; a morada vive noutra tabela para não chegar à fila pública · a taxa é configurada e copiada no pedido, nunca inventada · conector externo desligado com `CHECK`, e o mesmo evento duas vezes é UM pedido · takeaway e delivery são DUAS capacidades · `provar-mais-tarde.sh` a 0 com **onze controlos negativos** · **as 7 telas ESTÃO feitas** (STAFF-021, TAKE-001/002/003, DEL-001/002/003): a fila não calcula nada, pergunta à base, e o «já entrou?» vem do `now()` da base e não do relógio do navegador · o canal é um ARGUMENTO da mesma consulta e não uma tela separada, e por isso o filtro esconde da sala sem tirar da cozinha · a morada não chega à fila porque não está na consulta (garantia por AUSÊNCIA) · o formulário manda dia e hora SEPARADOS e a rota nunca constrói `new Date(...Z)` · `provar-levar-no-navegador.sh` a **0** com **24 casos** e **7 controlos negativos** · o `pnpm inspeccionar` apanhou **duas** guardas podres que NÃO eram do E20 e as duas mediam a circunstância e não o facto: o alvo `orderId` do arnês era `LIKE ... LIMIT 1` **sem ordem** com nove pedidos a casar (lotaria que passava por sorte da ordem física, seis bilhetes já antes do E20) e a guarda das 28 do E19 exigia a palavra `implementado aguardando validação`, ficando vermelha por a etapa ter AVANÇADO para `validado` · `provar-alvos-e-matriz.sh` a **0** com 2 controlos negativos · **pendência declarada:** `productId` (5), `menuId` (2), `categoryId` e `groupId` têm a mesma forma de lotaria e não lhes toquei · prova **LOCAL** |
| E21 | validado | docs/reviews/E21.md · **marco do Restaurant APROVADO à 2ª** — reprovado à 1ª por 3 bloqueios de ALCANCE (nenhum de correcção): sem porta para módulo nenhum, domínio próprio desligado, jornadas não percorríveis · sanado em `e5511b2` com 6 portas medidas por cliques e `provar-portas.sh` · 30 funções sem chamador triadas · 2 falhas de assinatura minhas |
| E22 | validado | docs/progress/E22.md · migrações `e22_contas_e_pagamentos` e `e22_caixa` · **quatro entidades e nenhuma colapsada**: a conta é a obrigação, a tentativa saiu daqui, o pagamento é o confirmado, a devolução não é um pagamento negativo · o pagamento **não tem estado** — existir é estar confirmado · o estado da conta e o da caixa **derivam-se**, e o devido deriva-se por gatilho · a caixa: abrir, contar, fechar e reabrir são ACONTECIMENTOS e o rasto **é** o fecho; o único número que um humano escreve é o contado, e a diferença é a subtracção (a prova procura uma coluna de diferença no `information_schema` e exige zero) · os movimentos **não têm coluna de meio de pagamento**: tudo o que lá está é dinheiro · **a recusa da concorrência era da BASE e não do negócio** — com `Serializable` + cadeado a segunda cobrança caía com `40001` porque o retrato é tirado na primeira instrução, antes do cadeado; em `READ COMMITTED` recusa com `EXCEDE_O_DEVIDO` e a prova passou a exigir o motivo · **imutável sem reversão é uma armadilha**: a correcção é um registo novo, com índice único · `provar-contas.sh` **0** (39 casos, 9 controlos) · `provar-caixa.sh` **0** (19 casos, 8 controlos) · `provar-tpv-no-navegador.sh` **0** (19 telas, **25 casos**, 9 controlos) · **RETIDO e consertado:** as 19 telas nasceram sem porta — zero `href` para `/pos/` e o menu em `#`; a POS-001 passou a ser a ENTRADA do módulo em `/pos`, com escolha de unidade, e a prova navega da sessão iniciada até uma tela do TPV **por cliques**, com o controlo negativo de repor o `#` · `pnpm inspeccionar` **0** (560 casos) · `validar-dinheiro.sh` **0** — passou a reconhecer o dinheiro pela FORMA (sufixo `Menor`) depois de ficar cega a três campos desta etapa, e o controlo negativo dessa secção, que nunca existira, destapou um segundo buraco · **pendências declaradas:** nenhum provedor de pagamento real (sem webhook, sem assinatura — `CARTAO` é um meio registado, não uma integração) e nada de fiscal, que é o E24 · prova **LOCAL** |
| E23 | validado | docs/progress/E23.md · migração `e23_pagamentos_e_webhooks` · **a identidade é a do ACONTECIMENTO** e a garantia é um índice, não um `if já existe` — entre a procura e a inserção cabe o segundo processo · **não há máquina de transições**: guarda-se o estado que o PROVEDOR autoriza com o instante que ELE carimbou, e o estado deriva-se do conjunto, por isso a ordem de chegada não decide · a assinatura verifica-se ANTES de qualquer efeito, com `timingSafeEqual` sobre o corpo CRU, e a recusa **não diz porquê** · o segredo **não tem onde morar na base** e o conector não liga sem provedor E merchant · 12 telas, três públicas em `mesa/` porque `menu/` é a carta anónima · **três telas estavam contadas na população e nunca eram visitadas**, achado por um controlo negativo que ficou verde · `provar-adquirente.sh` **0** (21 casos, 8 controlos) · `provar-pagamentos-no-navegador.sh` **0** (24 casos, 7 controlos) · `pnpm inspeccionar` **0** (589 casos) · **PENDÊNCIA DECLARADA:** sem credenciais de adquirente — porta e provas determinísticas, e o pagamento real NÃO declarado pronto · prova **LOCAL** · **CORRECÇÃO 05/09:** a minha varredura de alcance leu o commit declarado e não a etapa — `receberWebhook` e `reconciliarComProvedor`, da fatia 1, ficaram **sem chamador**. Nenhum webhook do adquirente chega ao produto. Assinatura mantida com a correcção anexada em `docs/reviews/E23.md`; a porta é pedido no E24 |
| E24 | validado | docs/progress/E24.md · **ADR 0002 com fonte, data e artigos** — AEAT e BOE consultados a 2026-09-05; a régua recusou enumerar requisitos e eu fui às fontes escrever de onde tirei · **⚠ PENDÊNCIA EXTERNA:** requisitos concretos do regime **por confirmar na fonte antes do primeiro talão a um cliente real** — a Orden HAC/1177/2024 não foi lida, não há fornecedor homologado, e a entidade do piloto não está classificada · construiu-se a **FORMA** e não o formato: só é documento fiscal o que está `ACEITE` **e** com número do fornecedor; emitir duas vezes devolve o mesmo e a correcção é documento novo com os dois a ficar; a rejeição é estado com motivo; e apagar ou reescrever é recusado por gatilho · **achado:** apanhar a colisão de um índice dentro de uma transacção não serve (`25P02`) — passou a `ON CONFLICT DO NOTHING`, e o E23 funciona por acidente · `provar-fiscal.sh` **0** (20 casos, 8 controlos) · `provar-fiscal-no-navegador.sh` **0** (23 casos, 8 controlos) · `provar-portas-do-dinheiro.sh` **0** (10 casos, 8 controlos) · `pnpm inspeccionar` **0** (609 casos) · **RETIDO por ALCANCE e consertado:** o ciclo fiscal e o webhook do E23 nao tinham porta · prova **LOCAL** · **PENDÊNCIA EXTERNA declarada:** os requisitos do regime fiscal por confirmar na fonte antes de tocar num restaurante real (`DECISOES-DO-MATHEUS.md`) |
| E25 | validado | docs/progress/E25.md · contrato `docs/architecture/stock-e-fichas.md` escrito antes do código · migração `e25_stock_e_fichas` · **o stock não é uma coluna**: `saldo_mili` deriva-se dos movimentos e `stock_saldo_nao_se_escreve()` repõe o número de quem o escrever, como o `producao_em` do E20 · a quantidade do movimento é **sempre positiva** e o sinal vem do TIPO · quantidade é inteiro em **milésimos** com sufixo `Mili`, com guarda própria (`validar-quantidades.sh`) deliberadamente separada da do dinheiro · a ficha é uma **árvore** e o ciclo recusa-se **na escrita** (`ficha_recusa_ciclo()`, CTE recursivo) — em serviço um ciclo via-se como o sistema a parar, não como ciclo · **consumo ao SERVIR**: cancelado antes de produzir não consome, produzido e devolvido consome (devolvê-la não a repõe no frigorífico; para o lixo é QUEBRA), e servir duas vezes a mesma linha é UM consumo por índice único parcial · **o negativo é VISÍVEL** — das três saídas, impossível trava o serviço às 21h e faz a equipa inventar um ajuste, e a que se reprova é a silenciosa · **ACHADO DE ALCANCE, apanhado ANTES de declarar:** o `consumirPelaLinha` tinha **zero chamadores** — motor escrito, provado e desligado do produto; ligado em `transitarTarefa` na passagem a `ENTREGUE`, e o `actor` fica vazio porque o consumo não é acção de ninguém, é consequência de uma (quem serviu está no `production_events.actorEmail`) · **achado de medição:** a asserção das folhas media um NOME, e a tela mostra o identificador em cru quando não resolve — uma sub-receita a passar por insumo aparecia como UUID e a asserção ficava verde; passou a exigir que toda a folha resolva para um insumo · `provas/stock.test.ts` **0** (23 casos) · `provar-stock.sh` **0** (**10 controlos**, dois a apagar a CHAMADA e não o comportamento) · `validar-quantidades.sh` **0** (5 campos, 2 controlos) · `provar-stock-no-navegador.sh` **0** (**26 casos, 8 controlos**) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**632 casos**) · `varrer-alcance-da-etapa.sh 665b027 HEAD` **0 sem chamador** — e no caminho pagou-se a dívida do E01: o `esquecerPrisma` tinha zero referências em todo o repositório e foi apagado · prova **LOCAL** |
| E26 | validado | docs/progress/E26.md · contrato `docs/architecture/compras-e-fornecedores.md` escrito antes do código · migração `e26_compras_e_fornecedores` · **encomendado, recebido e facturado são TRÊS números** em três tabelas, porque são três factos ditos por gente diferente — as diferenças **derivam-se** e não há coluna `divergencia`; o estado é só ABERTA/FECHADA porque um `RECEBIDA` obrigava a escolher entre recebida e recebida a menos, e a escolha apagava a informação por que a tela existe · **só a RECEPÇÃO mexe no stock, garantido pela AUSÊNCIA**: o movimento ganhou `receipt_line_id` e não ganhou `purchase_order_line_id` nem `supplier_invoice_line_id`, mais o gatilho `compra_so_entra_por_recepcao()` e o índice único que faz do reenvio UMA entrada · **a unidade de compra não é a de uso**: `factor_mili` NOT NULL com CHECK > 0, conta inteira de ponta a ponta, 8 sacos × 25 kg = 200 kg e nunca 8 · **custeio ESCRITO**: média ponderada móvel derivada das entradas, ponderada e não PEPS porque a farinha do saco novo e a do velho estão no mesmo balde · **achado:** a PUR-003 rolava a 360 px e a causa não era o CSS — o JSX remove o espaço entre irmãos escritos em linhas separadas, e seis `<span>` ficavam um bloco sem quebra; a quebra passou a ser dada pela caixa e não por espaços no texto · **achado no arnês:** o `extrair-sql.py` leva dois argumentos e eu passei um, e o `2>/dev/null` engoliu o erro — o silenciador era metade do defeito · `provas/compras.test.ts` **0** (22 casos) · `provar-compras.sh` **0** (**9 controlos**, incluindo a encomenda a mover o stock) · `provar-compras-no-navegador.sh` **0** (**26 casos, 8 controlos**) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**655 casos**) · prova **LOCAL** |
| E27 | validado | docs/progress/E27.md · contrato `docs/architecture/consentimento-e-campanhas.md` escrito antes do código · migração `e27_crm_e_campanhas` · **aqui um defeito manda uma mensagem a quem não a pediu**, por isso a garantia central é um GATILHO: `envio_exige_consentimento_vivo` recusa gravar um envio de campanha a quem não tenha, NAQUELE INSTANTE, consentimento vivo naquele canal, e grava-se antes de despachar · **não há coluna «aceita campanhas» em modelo nenhum** — o consentimento é um acontecimento com origem e momento, e o estado deriva-se do último de cada (pessoa, finalidade, canal): quatro respostas, nunca uma · **a finalidade de serviço ACABA** (`expira_em`, verificado contra o relógio da base) · a retirada vale antes do próximo envio porque a pergunta se faz POR PESSOA no instante de gravar cada um · audiência por regra: sem tabela de membros e sem caminho de importação · pontos derivados por gatilho, como o stock · **achado:** `25P02` numa forma nova — a excepção do gatilho aborta a transacção e apanhá-la em JS não a desaborta; no E24 a saída foi `ON CONFLICT`, aqui não há nenhum, e a pergunta passou a fazer-se ANTES com a mesma função SQL · **achado sobre as minhas asserções:** duas mediam coisas que não podiam falhar (a população aceitava qualquer pessoa; o contador lia o array e não o ecrã), e foram os CONTROLOS a cair no caso errado que as destaparam · `provas/crm.test.ts` **0** (27 casos) · `provar-crm.sh` **0** (9 controlos) · `provar-crm-no-navegador.sh` **0** (**38 casos, 10 controlos**; a MENU-017 medida SEM sessão, noutro ficheiro) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**690 casos**) · **PENDÊNCIA DECLARADA:** sem provedor de envio, os envios ficam `POR_ENVIAR` — a recusa é na gravação, que é o passo que existe · prova **LOCAL** |
| E28 | planejado |  |
| E29 | planejado |  |
| E30 | planejado |  |
| E31 | planejado |  |
| E32 | planejado |  |
| E33 | planejado |  |
| E34 | planejado |  |
| E35 | planejado |  |

Use estados: planejado; em execução; implementado aguardando validação; validado; bloqueado por dependência. Não transforme cobertura de design em implementação pronta.
