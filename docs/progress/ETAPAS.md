# Estado inicial

Os PDFs, contratos e prompts estão preparados. Nenhuma etapa de implementação foi executada nesta entrega. Copie esta referência para docs/progress quando iniciar o repositório.

| Etapa | Status | Evidência |
| --- | --- | --- |
| E00 | validado | docs/reviews/E00.md — **julgado pelo JR a 05/09, não por quem o escreveu** · 17 contratos a 03/09; hoje **11 intactos, 6 emendados** (o dossiê media 15/2 ao fim de 13 etapas — a erosão continuou) · critério cumprido: `dinheiro.md` de 03/09 ainda decide código do E26 por via do `validar-dinheiro.sh` que dele descende · a prova mais forte é a retenção em `capacidade-e-reservas.md`: o contrato deu DUAS saídas, quem implementou escolheu a da leitura e escreveu porquê, e o varredor ficou redundante em vez de em falta · **contra, e pior do que o dossiê dizia:** o fuso não passou por baixo de contratos que calavam o assunto — passou por baixo da secção Tempo do `capacidade-e-reservas.md`, que desde 03/09 nomeia a hora que não existe e a hora ambígua; o contrato estava certo e era INERTE · **a frase «contrato escrito antes reduz defeitos» fica REPROVADA** por não poder ser desmentida, e reescrita: só impede quem disser a consequência operacional e a regra de decisão NA FRONTEIRA, ou quem ganhar verificador executável; e nenhum contrato apanha função sem chamador — isso é mecanismo |
| E01 | validado | docs/progress/E01.md · 16 testes · provar-separacao-de-credenciais.sh e provar-prontidao.sh a 0 falhas |
| E02 | validado | docs/progress/E02.md · 2ª declaração após docs/reviews/E02.md · 69 testes unitários + 69 no browser · 12 capturas · STATE-001/002/003/005/007/016 |
| E03 | validado | docs/progress/E03.md · 2ª declaração após docs/reviews/E03.md · 85 testes + 28 asserções de isolamento + 12 verificações no provar-isolamento.sh (inclui autoteste do verificador) · zero telas, coverage intacto |
| E04 | validado | docs/progress/E04.md · 3 migrações · **127 asserções unitárias à data da validação** (domain 38 · ui 37 · i18n 16 · config 14 · auth 12 · db 5 · storage 5; worker declarado a zero) + 24 de acesso por HTTP + 13 de recuperação e MFA + 4 de fuso · provar-acesso.sh a 0 com o par a colapsar e provar-recuperacao-e-mfa.sh a 0 com três controlos negativos · 12 telas |
| E05 | validado | docs/progress/E05.md · 3 migrações (planos, descida agendada, plataforma) · **133 asserções unitárias** (domain 38 · ui 39 · i18n 16 · config 14 · auth 12 · db 5 · storage 5 · worker 4; **nenhum pacote declarado a zero**) + 17 de planos + 14 de descidas + 10 de plataforma · provar-planos.sh, provar-descidas.sh e provar-plataforma.sh a 0, com nove controlos negativos ao todo · 12 telas |
| E06 | validado | docs/progress/E06.md · 4 migrações (onboarding, criar organização, quota de marcas) · **motor de horários com três respostas** e lista de arranque com quatro estados · provar-onboarding.sh a 0 com 4 controlos negativos + guarda de limpeza · 14 telas |
| E07 | validado | docs/progress/E07.md · migração `e07_catalogo` (15 tabelas, allergens só de leitura para o runtime) · **ausência de declaração vale DESCONHECIDO por tipo, não por convenção** · empate de preços recusa em vez de escolher · modificadores validados por chamada directa à API, com os limites lidos da base · dinheiro em inteiros de unidade mínima · `provar-catalogo.sh` a 0 com 7 grupos, 21 asserções e 6 controlos negativos · guarda nova `validar-classes.sh` · 18 telas |
| E08 | validado | docs/progress/E08.md · migração `e08_media_traducoes_importacao_publicacao` (8 tabelas; `menu_revisions` sem UPDATE nem DELETE para o runtime) · **publicar é atómico por construção** — a revisão e a troca do ponteiro no mesmo COMMIT · CSV neutralizado, SVG recusado pelos bytes, três portas contra a rede interna · nome igual nunca funde produtos · exportação verificada duas vezes · 221 asserções no domínio + 35 contra a base, **34 controlos negativos** · retido à 1ª: o `redirect: manual` era uma protecção sem vigia, fechada com o controlo 9d · 10 telas |
| E09 | validado | docs/progress/E09.md · provar-publico.sh: 11 passos, **sete grupos, 26 casos, dez** controlos negativos · retido uma vez pela detecção de verbo que só via uma forma de escrever POST, fechada com onze formas · endereço público que não volta ao mundo, com o par que o separa da regra preguiçosa (o dono retoma) · **ASSINATURA PARCIAL fechada a 04/09**: as 5 telas que exigiam sessão (CHAN-001, QR-001/003/004, REP-001) foram medidas no E10, quando o arnês passou a autenticar · a correcção da fuga entre unidades ganhou detector próprio (grupo 7) depois de se descobrir que a própria prova a repunha na base a cada passagem |
| E10 | validado | docs/progress/E10.md · provar-sites.sh: 11 passos, 27 casos, **8 controlos negativos** — rascunho que sai, retirada por bandeira, idempotência do lead, **o `catch` largo**, domínio largado, a **regra preguiçosa** e a porta que serve sem prova de controlo · **254 casos de navegador corridos pelo revisor** a 360/390/768/1280/1440 · as 29 telas com móvel medido, e as **5 do E09 saíram da dívida** — condição bloqueante cumprida |
| E11 | validado | docs/reviews/E11.md · **marco do Starter APROVADO à 2ª** · reprovado à 1ª com **seis** falhas, todas de MEDIÇÃO e duas de assinatura minha · fechadas e reprovadas por `scripts/provar-marco-e11.sh`, que responde às seis pelo nome · 112/112 telas validadas **e** medidas em móvel · recusa entre inquilinos vista no ecrã nos dois sentidos · jornada percorrida de organização inexistente até site publicado · identidade por porta estreita com os dois consertos errados plantados |
| E12 | validado | docs/progress/E12.md · **validado à 2ª** — a 1ª assinatura foi retirada por eu ter assinado sobre um verde só local · a CI passa agora o trabalho do navegador, que era o ambiente que me contradizia · 7 controlos negativos na base e 22 casos no navegador com a cor **calculada** · a prova deixou de HERDAR o plano: estabelece-o e **verifica-o antes de medir** |
| E13 | validado | docs/progress/E13.md · **validado à 2ª** — retido por a junção a `users` do `ORG-007` estar viva pela **terceira** vez, no `responsavel` do `salaAgora` · fechada por estrutura com `validar-juncao-identidade.sh`, que a encontrou um minuto depois de existir · aceite 1 atacado pelo revisor **contra a base**, nos dois lados: segunda sessão recusada pelo índice único **parcial**, e a mesa reabre depois de FECHADA · limpeza como estado da sessão, não coluna da mesa |
| E14 | validado | docs/progress/E14.md · 2 migrações (`e14_pedidos` com 5 tabelas, RLS, envios e eventos **append-only por privilégio**, e o gatilho `linha_aceite_imutavel`; `e14_combos` com a restrição `componente_de_combo_nao_tem_preco`) · `provar-pedidos.sh` a 0 com **3 grupos, 21 casos e OITO controlos negativos** · idempotência por **restrição única** na base — o reenvio depois do commit devolve a MESMA resposta, e duas chaves diferentes continuam a criar dois pedidos · linhas **acrescentadas** e nunca reescritas em bloco; conflito **recuperável** com o que mudou e por quem · preço **copiado** para a linha aceite, com gatilho a recusar alterá-lo · **18 telas** (a régua diz 20; a matriz tem 18 e as outras 3 são telas anteriores a rever) com móvel medido · **a pergunta de dinheiro fica respondida**: o preço é o do servidor ao aceitar, e a divergência é rejeitada com o motivo em vez de aplicada em silêncio |
| E15 | validado | docs/progress/E15.md · `packages/fila`: partição por org+unidade+utilizador **como chave do armazém**, «não enviado» distinto de «pendente», gravação a cada transição, consulta-antes-de-repetir, portão de sessão · `provar-fila.sh` a 0 com **11 grupos, 24 casos e DEZ controlos negativos** · **23 telas** medidas no navegador às cinco larguras, alvos de operação a 48 px, contraste e três línguas — e a prova **conta a própria população** (23 telas, 23 ids, comparados contra a matriz) · `provar-staff-no-navegador.sh` com **cinco defeitos plantados no artefacto real** · PWA: manifesto por unidade e *service worker* servido de dentro do âmbito, que **nunca guarda HTML autenticado** — num tablet partilhado isso era a regra 3 quebrada por uma cache · `devices.rascunhos_por_enviar` **ligado à fila**, fechando a pendência que o E13 declarou · divergência de preço do E14 **visível no ecrã do empregado**, com os dois números lado a lado · prova **LOCAL**: a CI está trancada por facturação |
| E16 | validado | docs/progress/E16.md · migração `e16_producao_e_estacoes` (4 tabelas, RLS, **três gatilhos** — o estado de produção recusado no pedido, a versão da tarefa monótona, o carimbo de criação do servidor — e o `CHECK` de um alvo por regra) · **a unidade da estação é a TAREFA**: um-para-muitos desde o primeiro dia, e o modelo ingénuo é o primeiro controlo negativo · ausência de regra dá **não encaminhado**, nunca «cozinha por omissão» · o estado de produção do pedido **deriva** e a base recusa quem o escrever · o limite visível é regra de ECRÃ e não entra na consulta · os temporizadores contam do relógio da **base**, nunca do tablet · `provar-producao.sh` a 0 com **11 controlos negativos** e `provar-kds-no-navegador.sh` a 0 com **sete** · **20 telas** medidas às cinco larguras, alvos de operação a 48 px, contraste e três línguas, com a população lida da matriz · prova **LOCAL**: a CI está trancada por facturação |
| E17 | validado | docs/progress/E17.md · migração `e17_qr_da_mesa_e_visitante` (`guest_sessions`, RLS, e **três portas estreitas**: `visitante_activo`, `mesa_do_qr`, `abrir_visitante`) · **rodar não é revogar**, e a garantia é um campo que NÃO existe — a sessão de visitante não guarda a geração do QR, logo a comparação que faria a rotação expulsar gente não tem o que comparar · **mesa fechada com QR válido não abre sessão**: a porta só devolve linha com sessão de mesa ABERTA · o estado do visitante é DERIVADO · pedido de visitante é sempre de origem CARTA · **chamar a sala e pedir a conta** com limite, deduplicação e confirmação: limite e dedup são a MESMA janela de 120 s, por **MESA** e não por telemóvel (uma fotografia do QR abre telemóveis sem conta), na porta `chamar_a_sala` em plpgsql com `FOR UPDATE`, e quem chama vê **três respostas distintas** — avisámos agora / já tínhamos avisado / alguém vem a caminho · a bolacha do visitante tem `path=/r/<slug>` e a porta mudou para dentro desse caminho: fora dele o navegador não a enviava e **todos** os POST do visitante caíam em silêncio · `provar-visitante.sh` a 0 com **nove controlos negativos** — o obrigatório verifica também que o caso da revogação **não** cai junto — e `provar-visitante-no-navegador.sh` a 0 com **nove** · **16 telas** medidas às cinco larguras, com o caminho final afirmado em cada visita, que é o que separa medir a tela de medir o desvio · correcção de arnês transversal: o fecho da semeadura reconhecia o seu lixo pelo nome e os **pedidos** entram pela porta real com número da sequência — 398 ficaram na base a prender as estações, e a auto-verificação, calibrada pelo mesmo crachá, dizia «nada ficou para trás» · prova **LOCAL**: a CI está trancada por facturação |
| E18 | validado | docs/progress/E18.md · migração `e18_reservas_e_capacidade` (8 tabelas, RLS, e a garantia central na FORMA: `EXCLUDE USING gist (table_id, tstzrange(inicio, fim, '[)'))` — o `'[)'` **é** a regra do intervalo semiaberto, e não existe outro sítio onde possa ser escrita ao contrário) · **a unidade de alocação é a MESA**: uma combinação escreve uma linha por componente, e «3+4 e depois só a 3» bate na mesma exclusão sem caso especial · o **lock por unidade** guarda o que nenhuma restrição exprime, a CONTAGEM de comensais por zona · **o controlo obrigatório mudou por medição**: sem lock mas com `serializable` o teste fica verde COM RAZÃO (o SSI apanha o mesmo desvio e o retry responde), por isso o controlo desliga a serialização inteira — e o cenário passou a pedir 2 e 4 para caírem em mesas DIFERENTES, porque 4 e 4 escolhiam a mesma e media-se a exclusão a chamar-lhe contagem · a exclusão só é alcançável por escrita directa, e tem dois casos que a exercem (recusa a sobreposição, aceita o encosto) · a retenção expira por RELÓGIO e o varredor é higiene · hora INEXISTENTE e AMBIGUA resolvidas pela base, com o estado devolvido para o ecrã poder perguntar · depósito desligado por decisão, com `CHECK` a segurá-lo · `provar-reservas.sh` a 0 com **dez controlos negativos** e `provar-reservas-no-navegador.sh` a 0 com **seis** · **6 telas** medidas às cinco larguras, com guarda de população porque uma lista vazia é o ecrã fácil · prova **LOCAL**: a CI está trancada por facturação |
| E19 | validado | docs/progress/E19.md · migração `e19_lista_de_espera` (estado `SENTADO`, carimbos com `CHECK` a exigi-los, e `waitlist_areas` para as zonas que servem o grupo) · **a garantia é uma AUSÊNCIA**: não há coluna `posicao`, `numero_na_fila` nem `senha`, e há um caso a ler o `information_schema` mais um controlo que põe a coluna de volta · a posição **deriva-se** e agrupa pelo CONJUNTO DE MESAS que serve o grupo, com a preferência de zona na mesma chave — «o 2.º dos grupos de 5 ou 6» sobrevive ao de 2 passar à frente · quem não cabe em mesa nenhuma tem posição `null`, e não o último lugar · o par provado nos dois sentidos: sentar o de 2 muda a posição do outro de 2 **e não muda** a do de 6 · o controlo obrigatório do contrato acende e está **confinado** pela sugestão ao host, que tem de ficar verde · a estimativa nunca viaja sem `estimativa: true` ao lado · `provar-espera.sh` a 0 com **oito controlos negativos** · **11 das 28 telas feitas** (o fluxo público: PUB-003 e RES-C-001..010), medidas às cinco larguras com 9 controlos negativos · a porta da reserva pública fica FORA de `/r/` por decisão escrita — o que justifica o âmbito no endereço é haver uma credencial que não pode viajar, e um formulário anónimo não tem nenhuma · limite por unidade e janela com lock, chave idempotente nascida no HTML, e a recusa a levar ALTERNATIVAS consigo · a estimativa e o facto medidos no TEXTO, e a estimativa desaparece quando a mesa está pronta · **22 das 28 telas feitas**: o fluxo público (11) e o host (11), mais o `FLOOR-006` REVISITADO · migração `e19_chegada_e_lugar`: **chegar não é estar sentado** — dois actos, dois carimbos, e a base exige `chegou_em` em CHEGOU e em SENTADA · a reserva que aí vem aparece na sala numa mesa MESMO livre, com o par (a de amanhã não aparece, a já sentada deixa de ser anunciada) · as atrasadas são DITAS e não varridas, e a agenda di-lo por palavras · a definição das chegadas está ao lado do número · `provar-host.sh` a 0 com **sete controlos** e `provar-host-no-navegador.sh` a 0 com **seis** · **as 28 telas feitas**, mais o `FLOOR-006` revisitado · a mensageria: a MENSAGEM existe uma vez por (reserva, tipo) e as TENTATIVAS penduram-se nela, logo reenviar acrescenta uma tentativa e a entrega é um carimbo que não se escreve duas vezes — com o par, uma mensagem DIFERENTE para a mesma reserva É enviada · o conector nasce desligado e `activo` sem `provedor` não é um estado (`CHECK`), e sem provedor a mensagem fica PENDENTE, nunca ENVIADA · cada número do relatório traz a definição ao lado, e as duas telas que o mostram dizem o mesmo · `provar-mensagens.sh` a 0 com **sete controlos** e `provar-mensagens-no-navegador.sh` a 0 com **seis** · `pnpm inspeccionar` a **517 casos verdes** · pendência declarada: nenhuma mensagem foi entregue a ninguém, não há provedor · prova **LOCAL**: a CI está trancada por facturação |
| E20 | validado | docs/progress/E20.md · migração `e20_pedidos_para_mais_tarde` · **o momento de produção é DERIVADO** (hora de entrega menos preparo) e um gatilho substitui o que venha de fora · o preparo é o MAIOR das linhas e não a soma, porque as estações trabalham em paralelo · a passagem é por RELÓGIO e o relógio é o da BASE; a tarefa existe desde o aceite, logo atravessar o momento duas vezes não cria duas entradas · a hora de entrega passa por `resolverHoraLocal` e **sem fuso recusa-se**; uma hora que não existe é recusada, não empurrada · **a prova mede hora ABSOLUTA contra o relógio da base**, com o par dos dois fusos — nunca minutos relativos · o canal é uma DIMENSÃO (`DELIVERY` no enum) e não uma tabela; a morada vive noutra tabela para não chegar à fila pública · a taxa é configurada e copiada no pedido, nunca inventada · conector externo desligado com `CHECK`, e o mesmo evento duas vezes é UM pedido · takeaway e delivery são DUAS capacidades · `provar-mais-tarde.sh` a 0 com **onze controlos negativos** · **as 7 telas ESTÃO feitas** (STAFF-021, TAKE-001/002/003, DEL-001/002/003): a fila não calcula nada, pergunta à base, e o «já entrou?» vem do `now()` da base e não do relógio do navegador · o canal é um ARGUMENTO da mesma consulta e não uma tela separada, e por isso o filtro esconde da sala sem tirar da cozinha · a morada não chega à fila porque não está na consulta (garantia por AUSÊNCIA) · o formulário manda dia e hora SEPARADOS e a rota nunca constrói `new Date(...Z)` · `provar-levar-no-navegador.sh` a **0** com **24 casos** e **7 controlos negativos** · o `pnpm inspeccionar` apanhou **duas** guardas podres que NÃO eram do E20 e as duas mediam a circunstância e não o facto: o alvo `orderId` do arnês era `LIKE ... LIMIT 1` **sem ordem** com nove pedidos a casar (lotaria que passava por sorte da ordem física, seis bilhetes já antes do E20) e a guarda das 28 do E19 exigia a palavra `implementado aguardando validação`, ficando vermelha por a etapa ter AVANÇADO para `validado` · `provar-alvos-e-matriz.sh` a **0** com 2 controlos negativos · **pendência declarada:** `productId` (5), `menuId` (2), `categoryId` e `groupId` têm a mesma forma de lotaria e não lhes toquei · prova **LOCAL** |
| E21 | validado | docs/reviews/E21.md · **marco do Restaurant APROVADO à 2ª** — reprovado à 1ª por 3 bloqueios de ALCANCE (nenhum de correcção): sem porta para módulo nenhum, domínio próprio desligado, jornadas não percorríveis · sanado em `e5511b2` com 6 portas medidas por cliques e `provar-portas.sh` · 30 funções sem chamador triadas · 2 falhas de assinatura minhas |
| E22 | validado | docs/progress/E22.md · migrações `e22_contas_e_pagamentos` e `e22_caixa` · **quatro entidades e nenhuma colapsada**: a conta é a obrigação, a tentativa saiu daqui, o pagamento é o confirmado, a devolução não é um pagamento negativo · o pagamento **não tem estado** — existir é estar confirmado · o estado da conta e o da caixa **derivam-se**, e o devido deriva-se por gatilho · a caixa: abrir, contar, fechar e reabrir são ACONTECIMENTOS e o rasto **é** o fecho; o único número que um humano escreve é o contado, e a diferença é a subtracção (a prova procura uma coluna de diferença no `information_schema` e exige zero) · os movimentos **não têm coluna de meio de pagamento**: tudo o que lá está é dinheiro · **a recusa da concorrência era da BASE e não do negócio** — com `Serializable` + cadeado a segunda cobrança caía com `40001` porque o retrato é tirado na primeira instrução, antes do cadeado; em `READ COMMITTED` recusa com `EXCEDE_O_DEVIDO` e a prova passou a exigir o motivo · **imutável sem reversão é uma armadilha**: a correcção é um registo novo, com índice único · `provar-contas.sh` **0** (39 casos, 9 controlos) · `provar-caixa.sh` **0** (19 casos, 8 controlos) · `provar-tpv-no-navegador.sh` **0** (19 telas, **25 casos**, 9 controlos) · **RETIDO e consertado:** as 19 telas nasceram sem porta — zero `href` para `/pos/` e o menu em `#`; a POS-001 passou a ser a ENTRADA do módulo em `/pos`, com escolha de unidade, e a prova navega da sessão iniciada até uma tela do TPV **por cliques**, com o controlo negativo de repor o `#` · `pnpm inspeccionar` **0** (560 casos) · `validar-dinheiro.sh` **0** — passou a reconhecer o dinheiro pela FORMA (sufixo `Menor`) depois de ficar cega a três campos desta etapa, e o controlo negativo dessa secção, que nunca existira, destapou um segundo buraco · **pendências declaradas:** nenhum provedor de pagamento real (sem webhook, sem assinatura — `CARTAO` é um meio registado, não uma integração) e nada de fiscal, que é o E24 · prova **LOCAL** |
| E23 | validado | docs/progress/E23.md · migração `e23_pagamentos_e_webhooks` · **a identidade é a do ACONTECIMENTO** e a garantia é um índice, não um `if já existe` — entre a procura e a inserção cabe o segundo processo · **não há máquina de transições**: guarda-se o estado que o PROVEDOR autoriza com o instante que ELE carimbou, e o estado deriva-se do conjunto, por isso a ordem de chegada não decide · a assinatura verifica-se ANTES de qualquer efeito, com `timingSafeEqual` sobre o corpo CRU, e a recusa **não diz porquê** · o segredo **não tem onde morar na base** e o conector não liga sem provedor E merchant · 12 telas, três públicas em `mesa/` porque `menu/` é a carta anónima · **três telas estavam contadas na população e nunca eram visitadas**, achado por um controlo negativo que ficou verde · `provar-adquirente.sh` **0** (21 casos, 8 controlos) · `provar-pagamentos-no-navegador.sh` **0** (24 casos, 7 controlos) · `pnpm inspeccionar` **0** (589 casos) · **PENDÊNCIA DECLARADA:** sem credenciais de adquirente — porta e provas determinísticas, e o pagamento real NÃO declarado pronto · prova **LOCAL** · **CORRECÇÃO 05/09:** a minha varredura de alcance leu o commit declarado e não a etapa — `receberWebhook` e `reconciliarComProvedor`, da fatia 1, ficaram **sem chamador**. Nenhum webhook do adquirente chega ao produto. Assinatura mantida com a correcção anexada em `docs/reviews/E23.md`; a porta é pedido no E24 |
| E24 | validado | docs/progress/E24.md · **ADR 0002 com fonte, data e artigos** — AEAT e BOE consultados a 2026-09-05; a régua recusou enumerar requisitos e eu fui às fontes escrever de onde tirei · **⚠ PENDÊNCIA EXTERNA:** requisitos concretos do regime **por confirmar na fonte antes do primeiro talão a um cliente real** — a Orden HAC/1177/2024 não foi lida, não há fornecedor homologado, e a entidade do piloto não está classificada · construiu-se a **FORMA** e não o formato: só é documento fiscal o que está `ACEITE` **e** com número do fornecedor; emitir duas vezes devolve o mesmo e a correcção é documento novo com os dois a ficar; a rejeição é estado com motivo; e apagar ou reescrever é recusado por gatilho · **achado:** apanhar a colisão de um índice dentro de uma transacção não serve (`25P02`) — passou a `ON CONFLICT DO NOTHING`, e o E23 funciona por acidente · `provar-fiscal.sh` **0** (20 casos, 8 controlos) · `provar-fiscal-no-navegador.sh` **0** (23 casos, 8 controlos) · `provar-portas-do-dinheiro.sh` **0** (10 casos, 8 controlos) · `pnpm inspeccionar` **0** (609 casos) · **RETIDO por ALCANCE e consertado:** o ciclo fiscal e o webhook do E23 nao tinham porta · prova **LOCAL** · **PENDÊNCIA EXTERNA declarada:** os requisitos do regime fiscal por confirmar na fonte antes de tocar num restaurante real (`DECISOES-DO-MATHEUS.md`) |
| E25 | validado | docs/progress/E25.md · contrato `docs/architecture/stock-e-fichas.md` escrito antes do código · migração `e25_stock_e_fichas` · **o stock não é uma coluna**: `saldo_mili` deriva-se dos movimentos e `stock_saldo_nao_se_escreve()` repõe o número de quem o escrever, como o `producao_em` do E20 · a quantidade do movimento é **sempre positiva** e o sinal vem do TIPO · quantidade é inteiro em **milésimos** com sufixo `Mili`, com guarda própria (`validar-quantidades.sh`) deliberadamente separada da do dinheiro · a ficha é uma **árvore** e o ciclo recusa-se **na escrita** (`ficha_recusa_ciclo()`, CTE recursivo) — em serviço um ciclo via-se como o sistema a parar, não como ciclo · **consumo ao SERVIR**: cancelado antes de produzir não consome, produzido e devolvido consome (devolvê-la não a repõe no frigorífico; para o lixo é QUEBRA), e servir duas vezes a mesma linha é UM consumo por índice único parcial · **o negativo é VISÍVEL** — das três saídas, impossível trava o serviço às 21h e faz a equipa inventar um ajuste, e a que se reprova é a silenciosa · **ACHADO DE ALCANCE, apanhado ANTES de declarar:** o `consumirPelaLinha` tinha **zero chamadores** — motor escrito, provado e desligado do produto; ligado em `transitarTarefa` na passagem a `ENTREGUE`, e o `actor` fica vazio porque o consumo não é acção de ninguém, é consequência de uma (quem serviu está no `production_events.actorEmail`) · **achado de medição:** a asserção das folhas media um NOME, e a tela mostra o identificador em cru quando não resolve — uma sub-receita a passar por insumo aparecia como UUID e a asserção ficava verde; passou a exigir que toda a folha resolva para um insumo · `provas/stock.test.ts` **0** (23 casos) · `provar-stock.sh` **0** (**10 controlos**, dois a apagar a CHAMADA e não o comportamento) · `validar-quantidades.sh` **0** (5 campos, 2 controlos) · `provar-stock-no-navegador.sh` **0** (**26 casos, 8 controlos**) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**632 casos**) · `varrer-alcance-da-etapa.sh 665b027 HEAD` **0 sem chamador** — e no caminho pagou-se a dívida do E01: o `esquecerPrisma` tinha zero referências em todo o repositório e foi apagado · prova **LOCAL** |
| E26 | validado | docs/progress/E26.md · contrato `docs/architecture/compras-e-fornecedores.md` escrito antes do código · migração `e26_compras_e_fornecedores` · **encomendado, recebido e facturado são TRÊS números** em três tabelas, porque são três factos ditos por gente diferente — as diferenças **derivam-se** e não há coluna `divergencia`; o estado é só ABERTA/FECHADA porque um `RECEBIDA` obrigava a escolher entre recebida e recebida a menos, e a escolha apagava a informação por que a tela existe · **só a RECEPÇÃO mexe no stock, garantido pela AUSÊNCIA**: o movimento ganhou `receipt_line_id` e não ganhou `purchase_order_line_id` nem `supplier_invoice_line_id`, mais o gatilho `compra_so_entra_por_recepcao()` e o índice único que faz do reenvio UMA entrada · **a unidade de compra não é a de uso**: `factor_mili` NOT NULL com CHECK > 0, conta inteira de ponta a ponta, 8 sacos × 25 kg = 200 kg e nunca 8 · **custeio ESCRITO**: média ponderada móvel derivada das entradas, ponderada e não PEPS porque a farinha do saco novo e a do velho estão no mesmo balde · **achado:** a PUR-003 rolava a 360 px e a causa não era o CSS — o JSX remove o espaço entre irmãos escritos em linhas separadas, e seis `<span>` ficavam um bloco sem quebra; a quebra passou a ser dada pela caixa e não por espaços no texto · **achado no arnês:** o `extrair-sql.py` leva dois argumentos e eu passei um, e o `2>/dev/null` engoliu o erro — o silenciador era metade do defeito · `provas/compras.test.ts` **0** (22 casos) · `provar-compras.sh` **0** (**9 controlos**, incluindo a encomenda a mover o stock) · `provar-compras-no-navegador.sh` **0** (**26 casos, 8 controlos**) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**655 casos**) · prova **LOCAL** |
| E27 | validado | **defeito aberto:** `crm.ts:428` converte `BigInt` de pontos para vírgula flutuante — o JR corrige antes de declarar o E28 · docs/progress/E27.md · contrato `docs/architecture/consentimento-e-campanhas.md` escrito antes do código · migração `e27_crm_e_campanhas` · **aqui um defeito manda uma mensagem a quem não a pediu**, por isso a garantia central é um GATILHO: `envio_exige_consentimento_vivo` recusa gravar um envio de campanha a quem não tenha, NAQUELE INSTANTE, consentimento vivo naquele canal, e grava-se antes de despachar · **não há coluna «aceita campanhas» em modelo nenhum** — o consentimento é um acontecimento com origem e momento, e o estado deriva-se do último de cada (pessoa, finalidade, canal): quatro respostas, nunca uma · **a finalidade de serviço ACABA** (`expira_em`, verificado contra o relógio da base) · a retirada vale antes do próximo envio porque a pergunta se faz POR PESSOA no instante de gravar cada um · audiência por regra: sem tabela de membros e sem caminho de importação · pontos derivados por gatilho, como o stock · **achado:** `25P02` numa forma nova — a excepção do gatilho aborta a transacção e apanhá-la em JS não a desaborta; no E24 a saída foi `ON CONFLICT`, aqui não há nenhum, e a pergunta passou a fazer-se ANTES com a mesma função SQL · **achado sobre as minhas asserções:** duas mediam coisas que não podiam falhar (a população aceitava qualquer pessoa; o contador lia o array e não o ecrã), e foram os CONTROLOS a cair no caso errado que as destaparam · `provas/crm.test.ts` **0** (27 casos) · `provar-crm.sh` **0** (9 controlos) · `provar-crm-no-navegador.sh` **0** (**38 casos, 10 controlos**; a MENU-017 medida SEM sessão, noutro ficheiro) · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**690 casos**) · **PENDÊNCIA DECLARADA:** sem provedor de envio, os envios ficam `POR_ENVIAR` — a recusa é na gravação, que é o passo que existe · prova **LOCAL** |
| E28 | validado | **assinatura em `docs/reviews/E28.md`** · docs/progress/E28.md · contrato `docs/architecture/ponto-e-escalas.md` escrito antes do código · migração `e28_equipa_e_ponto` · **mexe no salário de quem trabalha na casa**, e quem perde com um defeito é quem tem menos poder para o contestar · a marcação é IMUTÁVEL na base pela mesma `registo_imutavel()` do E22, e a correcção é um registo NOVO com autor e motivo obrigatório por CHECK · segundo gatilho a proteger o SENTIDO: uma correcção não troca a pessoa nem o tipo · a autocorrecção não se proíbe, distingue-se · previsto e real são dois números e sem escala não há diferença NENHUMA (não zero) · minutos inteiros com guarda própria `validar-horas.sh` · **REGRA DE FRONTEIRA escrita**: o dia de serviço é a data civil no fuso da unidade do instante menos o corte, resolvida na ESCRITA — escrita assim por causa do veredicto do E00 · **achado:** um controlo ficou VERDE com o defeito plantado, porque o caso corrigia para mais cedo e o emparelhamento usa a marcação mais antiga · **achado:** as telas mostravam UTC e a picagem das 18h07 aparecia como 16:07 — apanhado pela varredura, que deu com o `minutosNoDiaDeServico` sem chamador; ao ligá-lo, a guarda das rotas mandou as duas funções PURAS para `@bossaos/domain` · **achados no arnês:** a limpeza não conseguia apagar (a imutabilidade a defender-se), apagava pela ordem errada (RESTRICT), e uma limpeza que rebentava pendurava o node — um vermelho a parecer «ainda a correr» · `provas/ponto.test.ts` **0** (22 casos) · `provar-ponto.sh` **0** (**10 controlos**) · `provar-ponto-no-navegador.sh` **0** (**25 casos, 9 controlos**) · `validar-horas.sh` **0** · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**712 casos**) · **PENDÊNCIA DECLARADA:** sem cálculo de salário — converter tempo em dinheiro é convenção laboral por verificar · prova **LOCAL** |
| E29 | validado | docs/progress/E29.md · contrato `docs/architecture/conciliacao-e-fecho.md` (do sénior, escrito por FRONTEIRA e anterior ao código) · migração `e29_financeiro_e_conciliacao` · **um sistema financeiro errado dá números CERTOS**: não parte, soma bem uma realidade que não existe · **a identidade de uma linha não vem do banco** — derivada de conta, data-valor, montante, referência e a ORDEM NO DIA, com índice único na base; a ordem existe pelo PAR, porque duas linhas legítimas iguais no mesmo dia entram as duas e sem ela o detector apagaria factos reais · **nada se auto-confirma**, nem a 100: CHECK que exige autor e momento · **conciliado é DERIVADO**, terceira vez depois do stock e dos pontos, com um caso a procurar a coluna no `information_schema` · **fechar proíbe por gatilho e não copia totais**, e o ajuste nasce no período aberto a apontar para o fechado · reabrir exige motivo por CHECK · **moedas não se somam**: agrupa, ou converte com fonte e data · **achado:** QUATRO funções sem chamador — `importarExtracto`, `sugerirCorrespondencia`, `fecharPeriodo` e `converterTotal`; o núcleo estava escrito, provado e inalcançável, e o extracto só entrava pela semeadura · **três achados nos meus controlos:** um caso falhava antes de chegar à sua própria pergunta, um plante pela metade nunca criou o defeito, e um plante que não compila mede um ficheiro partido · `provar-financeiro-no-navegador.sh` **0** (**31 casos, 9 controlos**) · **RETIDO por PRECISÃO e consertado:** a rota convertia com `Number` uma cadeia validada que aceita trinta dígitos, e o `BigInt` do motor corria depois — a linha do banco entrava errada em silêncio, na única porta por onde entra dado externo. A cura foi a do E27: o tipo aceita a CADEIA e ela viaja inteira até ao `BigInt()`. O defeito estava numa SEGUNDA porta que não constava da retenção (`registar_despesa`), e o dinheiro passou a ter leitor próprio. O local que era seguro (`Number(b-a)` num comparador) mudou na mesma, porque seguro-com-explicação obriga a guarda a tolerar excepções · **e o meu primeiro caso media a coluna e não a precisão**: 25 dígitos caem fora do BIGINT, e a janela real é entre 2^53 e 2^63 · `provas/financeiro.test.ts` **0** (**28 casos**) · `provar-financeiro.sh` **0** (**13 controlos**) · `validar-dinheiro.sh` **0** SEM excepções · `pnpm verificar` **0** · `pnpm inspeccionar` **0** (**740 casos**) · **PENDÊNCIA DECLARADA:** sem contabilidade legal e sem leitor de formatos de banco · prova **LOCAL** |
| E30 | validado | docs/reviews/E30.md · medido em DUAS voltas: a 1ª parou por a máquina cair para PERIGO · 42 casos, 17 controlos · **o menu de gestão ficou sem uma única porta morta** | docs/progress/E30.md · contrato `docs/architecture/relatorios-e-agregacao.md` (do sénior, por fronteira, anterior ao código) · **primeira etapa em que o defeito muda o comportamento de uma PESSOA** e não do sistema: um relatório errado faz um gerente fechar um turno, com o código verde · **ausência não é zero** — nada devolve um número solto, tudo é `Medido<T>`, e as duas coisas escrevem-se diferente no ecrã lado a lado · **o denominador viaja com o numerador** e não existe `mediaDeMedias` · o período resolve-se POR UNIDADE com a regra do E28 · duas organizações não se somam, e há um caso que o verifica no código-fonte · **a última entrada morta do menu de gestão está fechada** (o `início`), e o ramo do `porConstruir` ficou código morto · **duas decisões de rota declaradas:** a REP-016 vive na raiz da org porque comparar unidades de dentro de uma é contradição, e a CAT-028 em `catalogo/duplicar` porque o segmento `brands/` não existe no produto · **achados:** escrevi por cima do `relatorios.ts` do E14 e apaguei seis exportações (criar ficheiro sem verificar é escrita destrutiva disfarçada de nova); uma guarda minha lia prosa em vez de código; a guarda das rotas voltou a mandar funções puras para o domínio como no E28; e **duas vezes um plante tirou UMA instância do caso e não o caso**, ficando verde · `provas/analitica.test.ts` **0** (14 casos) · `provar-analitica.sh` **0** (8 controlos) · `provar-analitica-no-navegador.sh` **0** (**28 casos, 8 controlos**) · `pnpm verificar` **0** · **⚠ PENDÊNCIA DE MÁQUINA:** `pnpm inspeccionar` NÃO correu sobre esta árvore — mac-health em PERIGO (61 MB livres) matou três corridas a meio, e uma delas deixou um plante do guarda de classes no `catalogo/page.tsx`, reposto do git · prova **LOCAL** |
| E31 | validado |  | docs/progress/E31.md · contrato `docs/architecture/kiosk-e-impressao.md` e régua `ALVO-E31.md`, ambos anteriores ao código · **a primeira etapa que acaba num pedaço de papel e num ecrã sozinho num corredor** — nos dois sítios o produto perde a capacidade de verificar o que afirma · migração `e31_kiosk_e_impressao` (4 tabelas, RLS, **3 gatilhos** — a identidade do envio derivada, a segunda via marcada no papel, o reiniciar que não abandona cobrança — e 3 CHECKs) + `e31_porta_do_kiosk` · **dois clientes seguidos não partilham nada porque os dados NUNCA foram juntos**: a `kiosk_sessions` não tem uma coluna com dados da pessoa, e o pedido refere-a pela `order_contacts` com CASCADE do lado dela · as TRÊS saídas são UMA função e uma rota · **«entregue à ponte» não é «imprimiu»**: o enum não tem `IMPRESSO`, o «não sei» DERIVA do tempo sem resposta, e o CHECK `resposta_do_aparelho_ou_nada` torna impossível confirmar sem o que o aparelho disse · a segunda via marca-se NO PAPEL e a marca é um NÚMERO, logo a garantia não depende da língua · o reiniciar não abandona cobrança indeterminada: o gatilho recusa e o kiosk fica PAUSADO · **sem hardware não se declara homologação**: matriz em `E31-HOMOLOGACAO.md` com todas as linhas POR TESTAR, nunca em branco, e a base sem booleano `homologada` · **11 telas** ligadas a navegação existente · **achados:** a porta devolvia o slug INTERNO e a carta do kiosk vinha vazia em silêncio — motor verde, e quem o viu foi o NAVEGADOR («o teste passa e não sai papel»); o meu controlo negativo deixou a base sem a restrição única (guião morto a meio, agora dentro do PostgreSQL); apanhar erro de unicidade dentro de uma transacção não podia funcionar; um controlo ficou verde e o buraco era da PROVA (faltava o cliente que se vai embora sem tocar em nada); um plante que não compila não é um controlo verde; a `rotas-com-porta` reprovou-me pela EXPLICAÇÃO de já não usar `comEscopo` · `provas/kiosk.test.ts` **21** · `impressao.test.ts` **15** · `provar-kiosk.sh` **8 controlos** · `inspeccao/kiosk.spec.ts` **20** · `provar-kiosk-no-navegador.sh` **5 controlos** · `pnpm verificar` **0** · **pendências declaradas:** homologação física (não há aparelho), o `deviceId` como capacidade-URL (um kiosk CRIA pedidos, ao contrário do recibo público — fecha-se com sessão de aparelho, decisão de contrato), e o `FORCE RLS` como observação · prova **LOCAL** |
| E32 | validado |  | docs/progress/E32.md · contrato `integracoes-e-cobranca-do-saas.md` e régua `ALVO-E32.md`, ambos anteriores ao código · **a primeira etapa em que um estranho fala com o sistema sem passar por tela nenhuma, e a primeira em que o defeito RENDE DINHEIRO a quem o encontrar** · migração `e32_integracoes_api_e_cobranca` (7 tabelas, RLS, gatilho `resolucao_so_pela_ligacao`, e a função privilegiada `aplicar_evento_de_cobranca`) + `e32_check_do_ambito_dava_nulo` + `e32_porta_da_chave` · **O ACEITE QUE DECIDE:** a `saas_billing_events` NÃO tem `organization_id` que o webhook possa preencher — tem `organization_id_alegado` (guardado para denunciar) e `organization_id_resolvido`, que só a função privilegiada escreve, sempre pela `saas_customers`; e as portas do `saas-publico.ts` **não recebem escopo**, portanto a rota não tem por onde escolher organização · **a fronteira do E05 sobrevive**: o REVOKE está repetido nesta migração de propósito, e um controlo concede escrita ao runtime e vê a prova acender · a chave mostra-se UMA vez (não há coluna onde caiba), tem prazo `NOT NULL` e âmbito por OPERAÇÃO declarado em cada rota · revogar corta já, sem cache · assinatura sobre o corpo cru · destinos internos recusados **e por redireccionamento** (`redirect: 'manual'`, revalidado a cada salto) · os dois dinheiros não somam e o tipo não tem `total` · **13 telas** ligadas a navegação existente · **achados:** um CHECK que dá NULL PASSA (`array_length('{}',1)` é NULL, e a restrição recusava zero em todos os casos menos o array vazio — o único que interessa); o navegador viu três coisas que o motor não podia ver (a leitura da chave batia na RLS e dava 401 com a credencial real; a INT-008 mostrava chaves INTEIRAS deixadas por um controlo negativo meu que sobrevivia à limpeza; e um controlo ficou verde porque a prova só usava chaves inventadas, que ambas as rotas recusam por igual); o `URL` normaliza `[::ffff:127.0.0.1]` para hexadecimal; havia duas versões da mesma função e a resposta certa foi APAGAR a morta; e a `rotas-com-porta` corrigiu-me na mesma coisa do E31 · `provas/integracoes.test.ts` **23** (duas organizações) · `integracoes.test.ts` **26** · `provar-integracoes.sh` **7 controlos** · `inspeccao/integracoes.spec.ts` **25** · `provar-integracoes-no-navegador.sh` **5 controlos** · `pnpm verificar` **0** · **pendências declaradas:** não há provedor de assinatura (o produto DIZ que não está ligado), não há cupões porque o preço do SaaS não existe em coluna nenhuma, e o envio de webhooks não tem trabalhador · prova **LOCAL** |
| E33 | validado |  | docs/progress/E33.md · contrato `plataforma-e-suporte.md` e régua `ALVO-E33.md`, ambos anteriores ao código · **a maior etapa do projecto, 19 telas, e a única em que o atacante do modelo de ameaça somos NÓS** — todas as outras deram poder a quem trabalha na casa; esta dá-o a quem vende o sistema, e até aqui protegemos o restaurante de enganos e de estranhos: aqui protegemo-lo de nós · 5 migrações · **as quatro condições da sessão de suporte, e nenhuma decorativa:** TEMPORÁRIA (`expira_em` NOT NULL, e não há coluna `activa` — estar viva DERIVA de não ter fim e não ter expirado), VISÍVEL AO INQUILINO (RLS por organização **para a casa a ver**, e é a única das quatro que não vive na base), COM ÂMBITO (CHECK, e verificado por operação), COM MOTIVO · **a política é DA CASA** e não há rota da plataforma para a mudar — a ausência é a garantia · **o rasto guarda a PESSOA:** um gatilho recusa `plataforma.%` assinado por um papel, e a PLAT-013 conta as que escaparam, porque a auditoria é append-only e um minuto com o gatilho desligado fica escrito para sempre · **O ACEITE QUE DECIDE — a fronteira do E05 sobrevive:** REVOKE repetido pela TERCEIRA vez, e a escrita passa pela `conceder_capacidade` SECURITY DEFINER, que escreve concessão E auditoria na mesma instrução · **segurança, privacidade e exportação não ficam atrás do plano:** um gatilho na `plan_capabilities` recusa oito capacidades, e a lista está em SQL porque uma constante muda-se num commit e ninguém repara · as **três portas mortas** do menu da plataforma fecham-se · a PLAT-019 NÃO tem botões (migração é o poder mais destrutivo, e falta-lhe formato verificado, plano de reversão e assinatura) · **achados:** o motor não conseguia abrir sessões — e isso era a regra a funcionar; o `@default(uuid())` do Prisma é do lado do CLIENTE e há **93 tabelas** com `id` sem omissão na base; a minha limpeza tentou apagar auditoria e a base recusou (append-only); o índice chamava-se «viva» e impede «por fechar», e a condição NÃO pode incluir `now()` porque não é imutável; a RLS mordeu um caminho de leitura pela segunda vez nesta sessão; e a asserção do contador media o AMBIENTE e não o produto · `provas/plataforma.test.ts` **28** · `plataforma.test.ts` **29** · `provar-plataforma.sh` **7 controlos** (verifica as 4 permissões e os 3 gatilhos) · `inspeccao/plataforma.spec.ts` **25** · `provar-plataforma-no-navegador.sh` **6 controlos** · `provar-separacao-de-credenciais.sh` **0** · `pnpm verificar` **0** · **pendências declaradas:** a retenção concreta depende de conselho jurídico (vazio é «não decidido»), não há ferramentas de migração, e não há base de conhecimento · prova **LOCAL**  · **VALIDADO 06/09 pelo sénior**, cada ponto medido e não lido: as quatro condições com sonda por restrição e o positivo primeiro; o rasto nas quatro direcções dentro de uma transacção desfeita (append-only, 1511 linhas antes e depois), incluindo o controlo de NÃO acusar quem passa ao lado; a fronteira do E05 com `GRANT` ao vivo (verde → vermelho nomeando-a → verde), e a descoberta de que a `entitlement_grants` tem RLS que a bloqueia MESMO com o `GRANT` aberto; a `platform_secrets` sem coluna nenhuma capaz de guardar valor; as oito capacidades protegidas recusadas uma a uma com uma banal a entrar · **RETENÇÃO PAGA:** a identidade do trabalho era `tipo:alvo:tentativa` SEM organização e o índice é global — duas casas colidiam. O JR corrigiu-me a premissa (`organization_id` é anulável, não NOT NULL — trabalho global não é de casa nenhuma) e deu a proveniência: forma copiada da `print_jobs` do E31, onde funciona porque o alvo é um UUID; aqui é texto livre, e a propriedade que fazia o padrão estar certo não transferiu · cura `coalesce(org,'plataforma'):tipo:alvo:tentativa`, medida nas cinco direcções, com o controlo **8b** a repor a colisão · **contra mim:** a régua nomeou o instrumento errado — a `provar-separacao-de-credenciais.sh` ficava verde com a fronteira aberta, cobertura presumida pelo nome; alargada em `5b3d576` · `validar-no-commit.sh` **0** sobre o COMMIT · `provar-plataforma.sh` **8 controlos** · navegador **25 casos, 7 controlos** | docs/reviews/E33.md |
| E34 | validado | **as 4 dívidas da varredura de alcance estão pagas — 06/09**, e não são etapa: `revogarConvite` ligado (`DELETE` na rota dos convites, `equipa.gerir`, auditoria, e não-pendente dá **404** e não erro) · `apagarExcepcao` ligado no motor **e no ecrã**, com a auditoria a registar qual caminho correu · as 3 portas mortas do menu da **plataforma** marcadas `porConstruir: 'E33'` — e `LigacaoDeNavegacao` passou a **união discriminada**, porque o tipo exigia `href` obrigatório enquanto o comentário dizia «não tem href»: o `'#'` era a mentira que o compilador OBRIGAVA a escrever (medido: `TS2322`) · a tela pública dos alergénios deixa de reimplementar a regra e chama `avisosPorAlergenio`, derivada do `avisoDeSeguranca` — **medido com plante**: `contem: []` faz um prato COM glúten mostrar-se como «ninguém declarou», e antes disto esse defeito não chegava ao ecrã · `validar-desfazer.sh` de **2 pares sem desfazer a 0** em 447 substantivos · **3 guardas consertadas de caminho**: `validar-portas-mortas.sh` acusava prosa (o meu próprio comentário), `validar-testes.sh` dava NÃO MEDI em `auth`/`config`/`db` por só conhecer `# pass N` e não `ℹ pass N`, e `validar-dinheiro.sh` engolia o erro ao ler o schema · **24/25 guardas verdes**, `pnpm verificar` **0** · **fica aberto:** `leadsDaUnidade` (escrever sem ler)  · **CORRECÇÃO 1 ENCAMINHADA AO JR, FECHADA:** o `fecharCaixa` lia o estado, fazia cinco validações e só depois criava o evento — sem tranca, e o estado DERIVA do último acontecimento, que é uma leitura e não uma trava. A prova que falha veio primeiro e **passou à primeira** com o defeito lá: `Promise.allSettled` mede INTENÇÃO de concorrência, e abrir uma transacção interactiva é ela própria uma ida à base — a primeira acabava antes de a segunda abrir. Medido com sonda (`pid` 54579/54580 sobrepostos): **dois FECHOS na mesma caixa**. Com um ENCONTRO entre as duas transacções a corrida ficou determinista e a prova vermelha (esperado 1, obtido 2) · cura em DUAS partes, e medi que não são a mesma dita duas vezes: `FOR UPDATE` na caixa antes de ler o estado (sem ela o dinheiro fica certo e o ECRÃ fica errado — `23514` em vez de `RecusaDaCaixa`) e o gatilho `um_fecho_de_caixa_de_cada_vez` (sem ele a BASE aceita dois FECHOS por SQL directo, que é a porta que a tranca não guarda) · **gatilho com tranca e não índice único**: a caixa reabre, e o que é único é o fecho POR CICLO — e o ciclo não existe como coluna, escrevê-lo seria inventar um número que se deriva · migração `e34_um_fecho_de_caixa_de_cada_vez` · `provar-caixa.sh` **0 falhas** (12 secções, controlos 10 e 11 novos) · **observação não tocada:** a rota do TPV traduz recusas com `/^[A-Z_]+:/` e `CAIXA_FECHADA` sem detalhe não tem dois pontos — o operador lê «erro» em vez do motivo, e vale para todas as recusas de caixa  · **CORRECÇÃO 2 ENCAMINHADA AO JR, FECHADA:** o `pt-BR.json` misturava ortografia europeia com brasileira — não é preferência, é o ficheiro a divergir do PRÓPRIO padrão: `activ` 7 contra `ativ` 34, `contacto` 3 contra `contato` 7, `excepç` 1 contra `exceç` 5, medido por mim antes de mexer · onze chaves, e a varredura mais larga (`óptim`, `acto`, `adopt`, `objecto`, `direc[cç]`, `proje[cç]t`) não achou mais nenhuma · **só valores, nunca nomes de chave**: `staffE15.dispositivosActivos` É o nome da chave e um `sed` de `activ` pelo ficheiro fora partia o pt-BR contra as outras duas línguas · depois: `activ` **0**, `contacto` **0**, `excepç` **0**, e as **2344 chaves idênticas nas três línguas** com o `git diff` a tocar um só ficheiro · **a sonda prova-se a si própria**: zero achados lê-se como «limpo» e como «cego», e com «Dispositivos activos» replantado ela apanha 1 e nomeia a chave · `packages/i18n` **19 casos, 0 falhas** · **fica dito e não fiz:** a paridade de CHAVES é guarda, a ORTOGRAFIA de cada língua não é medida por ninguém  · **CORRECÇÃO 3 ENCAMINHADA AO JR, J09 FECHADA:** a jornada da caixa existe e percorre-se — `provas/jornada.test.ts` passa de 3 jornadas/18 passos a **4/27**: abrir, vender com o dinheiro a ENTRAR na gaveta, saída com motivo, cartão por reconciliar, contar, o fecho a RECUSAR, reconciliar, ENCERRAR, e o segundo fecho recusado (a junta de ontem, agora dentro da corrente) · tudo pelas portas de uma pessoa, com o `registerId` do redireccionamento, o `attemptId` do ecrã e **o valor a contar lido do ecrã de fecho** · alavanca `SEM_CONTAGEM=1` no modelo do `SEM_MOEDA` · **ACHADO 1: a J01 estava vermelha desde o E33 e ninguém deu por isso** — o gatilho `rasto_guarda_a_pessoa` partiu o `plataforma.mjs`, que assinava `plataforma:$USER` sem `actor_id`; como é a ÚNICA porta para dar plano e concessões, nenhuma organização nova era habilitável. Cura: o guião diz quem opera (`--por` / `BOSSAOS_OPERADOR`, resolvido contra `users`, SEM omissão) e a jornada cria essa pessoa pela porta do produto, em conta diferente da do restaurante · **ACHADO 2, a observação da correcção 1 agora paga:** a rota do TPV traduzia recusas com `/^[A-Z_]+:/` e uma recusa SEM detalhe não tem dois pontos — `SEM_CONTAGEM`, `CAIXA_FECHADA` e `PAGAMENTO_NAO_E_DINHEIRO` chegavam ao ecrã como «erro» · **ACHADO 3:** a limpeza bate em 5 gatilhos que recusam DELETE ao próprio migrador; desligam-se e o passo 7 verifica os SEIS, com a contagem primeiro · **contra mim:** o meu controlo 11 da caixa era uma moeda ao ar — as três inserções partilham o `criado_em` (`now()` é o instante da TRANSACÇÃO) e o desempate cai no uuid; passou a escrever a ordem, três corridas seguidas a 0 · `provar-jornada.sh` **0 falhas** · `validar-jornada.sh` **0** (vê as duas alavancas) · **por fazer:** J08, J12, J14, J15  · **CORRECÇÃO 3b — J14 FECHADA:** `provas/jornada.test.ts` passa de 4 jornadas/27 passos a **5/34**. A J14 vive na porta do adquirente, a única onde alguém de fora afirma que DINHEIRO ENTROU e a única sem sessão · corrente: recusar assinatura forjada sem dizer porquê, cartão SINALIZADO, `CAPTURADO` com o facto PRESERVADO e reconciliado, reenvio sem duplicar, devolução, e reenvio da devolução sem duplicar o dinheiro que SAI · **O DEFEITO QUE ELA ENCONTROU: o reenvio devolvia 500 com o corpo vazio** — e o reenvio é o caso normal de qualquer adquirente. O `receberWebhook` apanhava o `P2002` em JS e o próprio comentário avisava que era frágil «no dia em que alguém acrescentar uma linha»; alguém acrescentou NA ROTA (a reconciliação lê `membership.findFirst` a seguir), o erro do índice ABORTA a transacção e a leitura seguinte rebenta com `25P02`. Um 500 põe o adquirente a martelar, que é o que a rota diz por escrito querer evitar. Cura: a forma do E24 que o comentário já nomeava — `ON CONFLICT DO NOTHING` via `createMany(skipDuplicates)` · **e medi porque é que ninguém tinha visto:** com o defeito reposto, `provar-adquirente.sh` dá **0 falhas e 21 casos verdes, incluindo o caso do reenvio** — a prova de segmento chama a função e não corre a linha seguinte, e a `portas-do-dinheiro` verifica que a rota MENCIONA `receberWebhook` lendo o ficheiro como texto · alavanca `REENVIO_COM_IDENTIDADE_NOVA=1` apontada à DEVOLUÇÃO e não à captura, porque no `estadoAutorizado` o capturado é ATRIBUIÇÃO e o devolvido é SOMA: identidade nova leva o devolvido de 40,00 a 80,00, a chave idempotente do reembolso inclui o montante, e nasce um SEGUNDO reembolso · a ordem das asserções decide o que a paragem diz: o DINHEIRO primeiro, o mecanismo a seguir · **contra mim:** a limpeza desligava uma LISTA escrita à mão e abortou ao bater numa tabela nova, deixando 5 gatilhos desligados na base — passou a descobri-los em `pg_trigger` e a correr dentro de `BEGIN/COMMIT`, e provou-se sozinha quando um `kill` a meio deixou 0 desligados · **pendências declaradas: a integração de pagamentos NÃO é configurável hoje** — `WEBHOOK_SEGREDO_*` não está definido em lado nenhum e nenhum ecrã mostra o `organizationId` que o webhook tem de trazer · `provar-jornada.sh` **0 falhas** (corrido sozinho na base) · `provar-adquirente.sh` **0** · `validar-jornada.sh` **0** (três alavancas) · **por fazer:** J08, J12, J15  · **CORRECÇÃO 7 ENCAMINHADA AO JR, FECHADA — e o número mudou depois de medir:** a `validar-plantes.sh` dizia 19 plantes em letra morta; são **TRÊS**. Os outros 16, e mais 25 que ela nem via, eram defeito dela — e cometia DUAS VEZES o defeito que existe para apanhar · **(1) só lia metade da população**: o padrão era `python3 - <<'TAG'` e há 162 blocos invocados como `plantar <<'TAG'` — media 172 de 334 e reportava como se fossem todos, sem os contar nem declarar · **(2) comparava o TEXTO-FONTE da âncora e não o valor**: um `antigo = "a\nb"` ficava com barra e `n` literais, que nunca casam com uma quebra de linha, e todo o plante multilinha era dado por morto — dos 44 «mortos», **36 estavam vivos** · (3) media o NOME da variável, exigindo `antigo`, e metade dos guiões chama-lhe `alvo` ou `agulha`; passou a ler a âncora do `assert ... in s` com a convenção antiga como recurso, e pô-la a SUBSTITUIR em vez de somar fez os legíveis caírem de 310 para 245 — apanhado por medir outra vez · **de 172 lidos/19 mortos/11 por ler a 320 vivos, ZERO mortos, 12 por ler** (os 12 são dinâmicos: ambiente, JSON, cópias em /tmp) · **os três mortos a sério:** o `provar-catalogo.sh` plantava na cópia da regra dos alergénios que o E34 tirou, e ACUSAVA o produto do defeito mais perigoso do projecto sem nunca o ter plantado — re-ancorado ao único sítio onde a regra vive, e medido: 0 falhas com o controlo a acender («caiu a asserção dos catorze desconhecidos»); o `provar-pedidos.sh` tinha o filtro de combo mudado para `relatorios.ts` e o plante saiu, porque o caso que ele nomeia é o CHECK da base; o `provar-portas.sh` ficou SEM matéria-prima (não há um único `porConstruir` no produto) e passa a DECLARAR em vez de acusar — **e fica dito que a asserção «dizem QUAL etapa» corre hoje sobre zero marcadores** · **o que impede o regresso: 32 guiões passaram a verificar o plante** pelo `plantar()`, que lê o código de saída — se a âncora mudou, a falha é do GUIÃO e diz-se assim · **atribuição corrigida com medição:** das cinco falhas que a revisão ligou aos plantes, só o catálogo era; `mensagens` passa, e `staff`/`tema`/`visitante` falham NO PASSO 1, com tudo ligado e antes de qualquer plante — são vermelhos a sério, não acusações falsas · `validar-plantes.sh` **0** · `provar-catalogo.sh` **0** · `provar-mensagens-no-navegador.sh` **0** · `pnpm verificar` **0**  · **CORRECÇÕES 12 E AS TRÊS JORNADAS — J08, J12 e J15 FECHADAS:** **8 jornadas, 58 passos, 0 falhas numa só execução**, com os quatro elos partidos a acender, a árvore limpa e os sete gatilhos do rasto repostos — a régua exige a regressão das anteriores na MESMA corrida, e é o que este número é · **correcção 12:** a `unidadeArquivadaId` passou a filtrar por `ORG_A` (acertava por população de UM, não por desenho) e a `validar-alvos-com-casa` ficou verde · **J08:** conta que nasce da CARTA com o preço LIDO, parcela em dinheiro, fecho recusado enquanto a soma não fecha, parcela por cartão confirmada pelo adquirente, documento fiscal, e o fechamento medido como PORTA que recusa; a armadilha (repetir a parcela) dá `EXCEDE_O_DEVIDO` · **refutei metade de um critério:** não existe objecto «divisão» — ela emerge de pagamentos parciais e o produto recusa a soma que não fecha no FECHAMENTO (`CONTA_POR_LIQUIDAR`); aceite pelo sénior · **e ele apanhou-me o inverso:** o primeiro passo prometia por comentário que o preço vinha da carta e usava um LITERAL, comparando a conta com o mesmo literal — prosa a descrever um mecanismo que o código não tem; e a J12 tinha a mesma família, a contar um `data-teste` que não existe · **J12:** concessão anterior, herança CONTADA contra a mãe (provada não-vazia primeiro), override com as DUAS cartas republicadas — sem republicar a mãe o controlo passava por ela estar velha · **dois achados:** quem é convidada para uma unidade VÊ a outra na lista mas NÃO consegue mexer nela, e o emparelhamento é acto da ORGANIZAÇÃO enquanto o aparelho é da unidade · **J15, e o defeito maior do dia: o acesso de suporte nunca conseguia ler NADA** — a rota corre por `comIdentidade` e a `orders` tinha só `organization_id = app_organizacao_actual()`: **404 para todos os pedidos de todas as casas**, funcionalidade inerte, e invisível porque nenhuma prova abre essa porta (zero referências a `api/plataforma/suporte`) · não curei sozinho por ser isolamento entre inquilinos; o sénior decidiu a terceira via que o E33 já tinha escolhido: **política FOR SELECT com `suporte_com_concessao_viva`**, organização vinda da LINHA e não do chamador, com a rota a MANTER a verificação explícita (é ela que dá o 403 honesto; sem ela «sem concessão» virava «sem linhas» e lia-se como 404) · **auditoria na MESMA instrução da leitura**, com a CTE do registo alimentada pela da leitura: sem linha lida não há linha escrita · **dois 500 que eram regras a funcionar**, curados: o reenvio do webhook (E23) e a segunda sessão por fechar (E33, `SESSAO_JA_VIVA` 422) — e a distinção que os separa é que **as recusas de gatilho vêm na mensagem e as de índice único vêm no `meta`** · `provar-jornada.sh` **0** · `validar-jornada.sh` **0** · `validar-plantes.sh` **0** · `validar-alvos-com-casa.sh` **0** · `validar-rls.sh` **0** · `pnpm verificar` **0**  · **CORRECÇÃO 13, de segurança, no que eu tinha acabado de escrever:** a primeira composição do `suporte_le_pedido` prendia leitura e rasto com `WHERE (SELECT count(*) FROM rasto) >= 0` — e **zero é maior ou igual a zero**: com uma sessão que não casa com nenhuma, o INSERT inseria zero linhas e **a leitura devolvia os dados sem deixar rasto**. Reproduzido por mim antes de corrigir, em réplica com transacção revertida: sessão que existe → dados e 1 rasto; sessão inventada → **os mesmos dados** e o rasto na mesma 1 · não havia fuga (o predicado guardava a leitura), havia um acesso sem rasto — e um segundo efeito: com sessão de OUTRO agente o registo nomeava esse outro · **e aquele WHERE era pior do que parecia:** uma CTE que escreve corre sempre e por inteiro, lida ou não, portanto a condição nunca teve função nenhuma — decoração com ar de mecanismo · cura: a sessão entra no `lido` com TRÊS condições (é a indicada, é da organização daquele pedido, é do agente que chama), e sem isso não há dados NEM rasto porque os dois saem da mesma linha · medido com a contagem em instrução separada, porque dentro da mesma o `count` não vê o próprio INSERT: inventada → nada/0, outro agente → nada/0, próprio → dados/1 · **controlo 1c** na J15 pelas duas pontas · `provar-jornada.sh` **0 falhas**, 8 jornadas, **59 passos**  · **CORRECÇÃO 4 — a CI descobre as provas:** o número da revisão estava errado e o sénior corrigiu-o antes de mo mandar (o `pnpm inspeccionar` é `playwright test` SEM `--project`, logo as 22 de navegador já corriam) — a pergunta certa é sobre FICHEIROS e não guiões · medido: 13 guiões nomeados, **40** ficheiros `provas/*.test.ts`, **12 alcançados, 28 nunca corriam** · **e o cruzamento dói:** dos sete ficheiros com os casos concorrentes das famílias saldo/reservas/deduplicação, **SEIS** estavam nos 28 (caixa, contas, pedidos, reservas, sala, sites) — os invariantes davam-se por provados e só se provavam à mão; na mesma lista estavam as provas do dinheiro · cura: `for g in scripts/provar-*.sh` como as guardas já faziam, com as excepções num FICHEIRO com motivo escrito (`scripts/provas-fora-da-ci.txt`) lido pela CI **e** pela guarda — antes eram duas listas com a mesma responsabilidade e nenhuma sabia da outra · **e caducam:** a `validar-provas-na-ci.sh` reprova uma entrada que nomeia guião inexistente, e uma que diz «corre por nome» quando o workflow já não a nomeia — provado nas DUAS formas com a entrada plantada e reposta · **de 12 para 39 dos 40 ficheiros** · **por medir:** a CI está trancada por facturação e NÃO a corri; corri a mesma descoberta localmente (32 guiões seleccionados, 38 fora) e as guardas · **achado à parte:** `provas/reserva-publica.test.ts` não é alcançada por guião NENHUM — há um ficheiro de prova sem corredor  · **CORRECÇÃO 5 FECHADA — e a pergunta só se respondeu com instrumento que funciona:** o `git grep -E` com `\b` dava zero para os dois porque **o `git grep` não suporta `\b`** — o zero era do instrumento; com `-w` são nove ficheiros e três · **o varredor:** `varrerRetencoesExpiradas` estava escrito, exportado e com teste, e com ZERO chamadas no produto — faltava a linha, não faltava o worker (o ciclo já corria o `varrerDescidas`) · risco baixo **por leitura e não por suposição**: a própria função distingue HIGIENE de correcção, a capacidade já fica livre sem ela, e o que evita é a lista de espera cheia de ofertas mortas no ecrã de quem trabalha · enumerador `unidades_com_retencoes_expiradas()` na forma do `descidas_devidas()`: SECURITY DEFINER que **recusa responder de dentro de um inquilino**, e devolve só as unidades COM trabalho · provado com o par (encontra, e deixa de listar depois de varrer) e o controlo da recusa · **o `sentar` apagado e não fundido:** era a METADE PERIGOSA do `sentarReserva` — mudava o estado sem exigir chegada, sem ver a mesa ocupada e **sem abrir a sessão** —, e fundi-lo preservava a armadilha com outro nome · **o que prova que nada partiu não é o `verificar` passar** (isso prova que ninguém o chamava): ficou um caso que exige a propriedade que ele violava — nenhuma reserva chega a SENTADA sem sessão de mesa aberta — com o controlo a replicar a metade perigosa e a exigir que a propriedade CAIA · `provar-reservas.sh` **0** (37 casos) · `provar-host.sh` **0** (19 casos) · `provar-descidas.sh` **0** · `validar-desfazer.sh` **0** em 504 substantivos · `pnpm verificar` **0** · **VALIDADO 07/09 pelo sénior, com varredura feita NO MOMENTO da assinatura e não de memória.** Aceite 1: as **oito jornadas** (J01, J02, J08, J09, J11, J12, J14, J15) numa só execução, 58 passos, com a régua `ALVO-J08-J12-J15.md` escrita ANTES do código — e duas exigências dela refutadas com razão por quem as implementou (não existe objecto «divisão» neste produto; as duas recusas da J15 não têm de dar a mesma razão, o que não pode haver é um 404). Aceite 3: as seis famílias com cenário concorrente — saldo, reservas e deduplicação já provadas (14 casos), e **reembolso, stock e acesso escritos nesta etapa**, com disparo sobre um ENCONTRO e a sobreposição medida em `pg_stat_activity` em vez de presumida: `adquirente` 24, `stock` 25, `plataforma` 37, zero falhas e **zero `todo`**. Aceite 4: acessibilidade uma a uma; **2343 chaves nas três línguas, zero em falta**, com os textos idênticos ao espanhol abertos um a um (16 de 3+ palavras, todos legítimos); **zero domínios de fantasia e zero texto de encher** nos 670 ficheiros que embarcam; e listas grandes com **zero** consultas sem tecto em caminho vivo, depois de cinco afunilamentos (161→139→16→8→2→0). Aceite 5: **os 128 itens abertos têm todos veredicto**, e a tabela separa CINCO naturezas que a lista de origem misturava numa palavra — dívida, decisões já tomadas, **objecções ao contrato** (um aceite mal formulado não é trabalho por fazer), dependências do mundo, e não verificáveis. **Prova da assinatura:** 34 guardas verdes, 0 falhas, 0 NÃO MEDI, cada uma provada a conseguir ficar vermelha; as 68 migrações aplicam-se do zero contra base vazia com o controlo negativo a partir a cadeia **na dependência certa**; as 8 jornadas verdes com os controlos a disparar. **14 correcções encaminhadas e fechadas**, e a 13 e a 14 são consertos de defeitos MEUS: escrevi o `JOIN` do rasto do suporte sem a liveness da sessão nomeada, e aprovei o comentário ao lado que promete o que o código não fazia — o rasto podia nomear uma sessão já fechada, com o motivo errado. **AS RESSALVAS, e são parte da assinatura:** (1) **tudo isto é verde nesta máquina** — a CI não corre desde 05/09 e há 190+ commits por empurrar, portanto nenhuma das guardas escritas hoje foi vista por ela; (2) a **dívida 2** (`/staff/` sem uma única ligação) continua a ser decisão do Matheus; (3) fica aberto quem escreve `PRO` na casa A durante a corrida do tema — o vermelho está explicado (premissa falsa do teste, produto correcto) e a proveniência não; (4) o `-g` do `provar-tema-no-navegador.sh` arrasta quatro casos alheios porque `tema` está dentro de `sistema`; (5) o `provas/reserva-publica.test.ts` — 13 casos que são o contrato público das reservas e correm em 0,7 s — continua sem guião que o corra. **Sete instrumentos meus mediram a coisa errada hoje e ficaram todos escritos em `docs/reviews/E34.md`**, porque um documento de fecho que só conta os acertos é o defeito que esta etapa existe para apanhar |
| E35 | validado | docs/progress/E35.md · contrato `docs/architecture/implantacao-e-piloto.md` e régua `ALVO-E35.md`, ambos anteriores ao código · **zero telas — a única etapa do projecto sem uma tela nova**, e a primeira cujo contrato não foi escrito por dedução: o sénior escreveu-o depois de publicar o staging com NOVE paragens · **os quatro portões, e nenhum é cerimónia:** assinatura (`etapasPorValidar`), commit (`arvoreLimpa` + `git archive` — o que sobe existe em git porque não há outra maneira de lá chegar), segredos (`podeSubir`), versão (`versaoQueResponde`, lida da etiqueta `bossaos.versao` PELO DOCKER e não através do produto — a sonda do sénior pedia `/versao.txt` e o encaminhamento por idioma respondeu `/es-ES/versao.txt`, medindo a casa em vez do build) · **preparar não é publicar:** sem `--autorizado-por` prepara, prova e PARA, e a autorização vai nas PALAVRAS de quem manda · **as decisões vivem no domínio e não no bash**, porque uma decisão em shell não se testa · **ensaio de restauração medido** 06/09 06:19 UTC, 0,56 s, com amostra verificada (produtos=5 pedidos=9 movimentos=7, 5 gatilhos repostos) e o guião a RECUSAR emitir RPO/RTO · **importação a seco que não abre ligação nenhuma** (não é «escreve numa base de teste»: é não falar com base nenhuma), lista de conferência antes de a casa aceitar, alergénio vazio vira aviso DESCONHECIDO e nunca «não contém» · **`pilot.md` com três estados** (feito/pendente/não medido) e zero células em branco · **entrada progressiva com plano de saída ANTES de subir**, e `degrauPodeSubir` recusa em código o degrau sem saída · **ACHADO QUE PESA MAIS QUE A ETAPA:** reescrevi por cima do `publicar.sh` do sénior e apaguei-o — o ficheiro tinha corrido a sério e levava a caixa, a chave, a porta verificada à mão, o `--env-file`, os papéis, a migração e as duas metades do portão 4; o que escrevi era uma demonstração que NÃO publicava nada. Defeito do E30 outra vez (criar ficheiro sem verificar é escrita destrutiva disfarçada de nova), agravado por eu estar a escrever uma etapa SOBRE publicar com rigor. Original reposto do git e os portões acrescentados DENTRO dele; três coisas minhas caíram por não caberem no que já existia, e a que interessa é esta: **a árvore limpa como PORTÃO estava errada** — o sénior não a exige de propósito, porque são dois agentes na mesma árvore, e a garantia é o `git archive` e não a regra (ficou como aviso). A etiqueta que inventei não existe: chama-se `bossaos.versao` no contentor `bossaos_web` · **e o medidor morre exactamente quando isto estiver pronto:** o `estado.sh` rebenta se não houver etapa por validar, e com `pipefail` + `set -e` matava o `publicar.sh` ANTES da primeira linha de saída — zero texto, código 1, indistinguível de um portão a recusar. Apanhado pela casa de mentira do controlo, que tem as 36 validadas. A falha do medidor passa a NÃO MEDI e o portão fica pela leitura directa da matriz · **outros achados:** o `podeSubir` deixava passar `.next` no MEIO do caminho (comparava prefixos, e o build inteiro ia para o ar — apanhado pelo meu próprio teste); o ensaio ficou VERDE sobre uma base vazia e o pré-voo novo apanhou-o outra vez hoje; o relógio mentia por resolução (560 ms escrevia-se `0s`, e um zero lê-se como promessa); o meu controlo deu FALHA nos quatro portões com os quatro a funcionar porque o `grep` casava com a formatação (`PARADO\033[0m texto`) e não com o comportamento; e o bash leu `«$e»` como variável chamada `e»`, matando o controlo a meio DEPOIS de já ter escrito verdes · **RETENÇÃO DO SÉNIOR PAGA:** o ponto 8 tinha duas metades que nunca se tocavam — o controlo contava `## Degrau ` contra `**Plano de saída` e o `degrauPodeSubir` corria sobre objectos INVENTADOS (`{nome:"x", saida:null}`); a regra estava certa e nunca via um degrau verdadeiro. Ele mediu o buraco esvaziando o plano do último degrau com o cabeçalho intacto: `degraus=3 saidas=3`, VERDE com o plano vazio. **Contar títulos mede que alguém escreveu o título.** Cura: `lerDegraus` lê o documento e cada degrau passa pela regra, com a `saida` a sair do TEXTO e não da presença do rótulo — senão reproduzia o defeito que veio corrigir. Medido com população primeiro (3 degraus lidos), sonda que tem de passar, o ficheiro adulterado dele a recusar, e o outro lado a não acusar os intactos; plante «tem rótulo logo tem plano» acende 4 falhas e reprova o guião · `packages/domain` **427 casos, 0 falhas** · `provar-implantacao.sh` **0 falhas** (cenário 0 que TEM de passar + os quatro portões violados À VEZ + 4 controlos, numa cópia ISOLADA para não depender do estado do dia) · **pendências declaradas:** não há servidor de piloto (o portão 4 nunca leu uma imagem a sério — o controlo prova a REACÇÃO à etiqueta, com um `docker` de mentira), não há casa piloto (seis guiões de formação escritos, ZERO pessoas testadas), e não há RPO nem RTO porque não há calendário de cópias · prova **LOCAL**  · **VALIDADO 06/09 pelo sénior** · os quatro portões recusam UM A UM, com a sonda positiva à cabeça, e o da versão distingue **etiqueta ilegível (NÃO MEDI)** de «versão errada» — e prova que com a etiqueta certa chega ao fim, sem o que «recusa sempre» passava por portão · ensaio de restauração com amostra verificada (0,56 s) e `podeDeclararRpo` a RECUSAR produzir RPO/RTO daí · `pilot.md` com 116 células e ZERO vazias · **retenção paga:** o ponto 8 tinha o controlo a contar cabeçalhos e a regra a correr sobre objectos inventados; agora `lerDegraus` passa os três degraus verdadeiros pela regra, e a saída sai do TEXTO e não da presença do rótulo · **erro meu no meio, corrigido:** a minha adulteração de prova não esvaziava nada (a linha do rótulo já carrega o plano); refi-la a sério e a cura confirma-se · **P3 anotado:** a saída pára só em `---`, portanto no último degrau ela absorve o que vier a seguir — latente, o documento real tem o separador · **achado dele contra si:** reescreveu por cima do `publicar.sh` e apagou-o, apanhou-se no `git diff`, e verifiquei a reposição · `validar-no-commit` **0** sobre o COMMIT · `provar-implantacao` **0** | docs/reviews/E35.md |

Use estados: planejado; em execução; implementado aguardando validação; validado; bloqueado por dependência. Não transforme cobertura de design em implementação pronta.
